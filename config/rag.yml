# RAG 与大模型配置。

# 聊天模型调用方式。
# 枚举：
# - openai_compatible：使用 OpenAI 兼容接口，适合百炼 compatible-mode/v1 以及其他兼容服务。
# - tongyi：使用 DashScope/通义原生接口；部分新模型可能和该封装不兼容。
# - openai：openai_compatible 的等价别名。
chat_provider: openai_compatible

# 最终回答模型名称。
# 作用：用于 Query Planner 的 LLM 改写，以及最终客服回答生成。
# 注意：模型名必须和 chat_provider 对应的接口兼容。
chat_model_name: deepseek-v4-flash

# 聊天模型档位映射。
# low：低延迟模式，适合日常快速问答。
# medium：平衡模式，适合稍复杂但仍希望响应较快的问题。
# high：高质量模式，适合复杂问题或更看重回答质量的场景。
chat_model_low: qwen-turbo
chat_model_medium: qwen3.6-flash
chat_model_high: deepseek-v4-flash

# OpenAI 兼容接口地址。
# 仅 chat_provider=openai_compatible/openai 时使用。
openai_base_url: https://dashscope.aliyuncs.com/compatible-mode/v1

# 文本向量模型名称。
# 作用：知识入库和问题检索时生成 embedding。
# 注意：更换向量模型或向量维度后，需要重建 Qdrant collection。
embedding_model_name: text-embedding-v4

# 聊天路由模式。
# 枚举：
# - direct_rag：普通聊天直接走“知识库检索 -> 模型回答”，首包更短，推荐默认。
# - agent：走 LangGraph Agent 工具链，会让模型判断是否调用工具，适合复杂工具编排但更慢。
# - react_agent：agent 的等价别名。
# - legacy_agent：agent 的等价别名，保留给旧配置兼容。
chat_route_mode: agent

# Query Planner 模式。
# 枚举：
# - adaptive：推荐。先用原问题检索，召回质量差才调用 LLM 改写/拆分。
# - llm：每次检索前都调用 LLM 改写/拆分，效果可能更强但首包更慢。
# - rule/rules/off/disabled：不调用 LLM，也不做关键词硬匹配，只用原问题和显式多问号拆分兜底。
query_planner_mode: adaptive

# Query Planner 可读取的最近历史消息条数。
# 作用：让模型拆分“那这个呢”“继续上面的问题”这类追问时能看懂上下文。
# 值越大上下文越完整，但 Query Planner 调用会更慢、消耗更多 token。
query_planner_history_limit: 20

# Query Planner 中每条历史消息最多保留的字符数。
# 作用：避免历史消息过长导致拆分模型变慢；只保留足够判断上下文的摘要片段。
query_planner_history_chars: 300

# adaptive 模式下，首次召回 top1 分数阈值。
# 低于该值会认为召回偏弱，触发 LLM Query Planner。
adaptive_retrieve_min_score: 0.72

# adaptive 模式下，首次召回最少资料数。
# 少于该数量会认为覆盖不足，触发 LLM Query Planner。
adaptive_retrieve_min_docs: 3

# adaptive 模式下，首次召回 top3 平均分阈值。
# 低于该值说明整体相关性不稳定，会触发 LLM Query Planner。
adaptive_retrieve_top3_avg_score: 0.68

# adaptive 模式下，是否启用“极低分跳过 Query Planner”。
# 作用：乱码、闲聊、无关问题的首次召回分数很低时，不再调用 LLM 改写，减少无效等待。
adaptive_skip_planner_on_very_low_score: true

# 极低分跳过 Query Planner 的最高分阈值。
# 只有 top1 分数低于该值时，才可能跳过模型改写。
adaptive_skip_planner_max_score: 0.55

# 极低分跳过 Query Planner 的前三平均分阈值。
# 只有 top3 平均分低于该值时，才可能跳过模型改写。
adaptive_skip_planner_top3_avg_score: 0.50

# 启动预热总开关。
# 作用：服务启动时提前初始化 RAG 关键依赖，减少第一次聊天的冷启动等待。
startup_warmup_enabled: true

# 是否预热 Qdrant 向量库连接。
# 作用：提前检查 collection 是否存在，并建立 Qdrant 客户端连接。
warmup_qdrant: true

# 是否预热 embedding 模型。
# 作用：启动时用短文本调用一次向量模型，提前完成鉴权、网络连接和模型侧初始化。
warmup_embedding: true

# 是否预热最终回答模型。
# 注意：开启后启动时会真实调用一次聊天模型，会消耗少量 token/额度；默认关闭。
warmup_chat_model: false

# 预热失败时是否中断应用启动。
# false：只打印错误日志，应用继续启动；true：预热失败则启动失败。
warmup_fail_fast: false

# 文档解析规则配置。
# 说明：这里集中配置文档切片时使用的结构识别规则，避免把“编号、标题、问答前缀”等格式写死在代码里。
# 注意：正则需要使用双引号，并把反斜杠写成 \\，否则 YAML 会把它当成转义字符。
document_parse_rules:
  # 编号条目行，例如“1. 问题”“2、问题”。
  numbered_item_pattern: "^\\s*(\\d+)[.、]\\s*(.+)$"

  # Markdown 标题行，例如“## 分类”“### 子分类”。默认不匹配一级标题，避免把整篇总标题当成分类。
  heading_pattern: "^\\s*#{2,6}\\s*(.+)$"

  # 只有编号前缀的行首片段，用于从题目标题里提取/移除题号。
  number_prefix_pattern: "^\\s*(\\d+)[.、]\\s*"

  # 同一行标题前方的编号前缀，用于 PDF outline 标题定位时把题号一起纳入命中范围。
  number_prefix_only_pattern: "^\\s*\\d+\\s*[.、]\\s*$"

  # 问答答案前缀，匹配后会从答案开头移除。
  answer_prefix_pattern: "^(?:答案|答|A)[:：]\\s*"

  # 无效答案占位，例如“答：”“A:”“问题：”这种只有标签没有内容的片段。
  invalid_answer_pattern: "^(?:问题|答案|答|Q|A)[:：]?$"

  # 判断标题像问题的标点。后续如果资料里常用英文问号或其他符号，可以只改这里。
  question_marks:
    - "？"
    - "?"
