# V2 重构整改复盘与尾巴清单

> 本文档用于记录本轮“大爆炸架构与页面治理重构”的实际整改范围、仍未清理的旧代码尾巴、后续建议执行顺序和验证方式。  
> 当前代码按用户要求保留在本地工作树，暂未提交。

## 1. 本轮目标

本轮目标不是简单修页面或局部补丁，而是把项目从历史迭代堆叠结构逐步迁移到 V2 分层架构：

- 后端统一走 `app_v2` 分层结构：路由层、应用服务层、仓储层、基础设施适配层。
- 前端按首页、智能客服、销售训练、问答考试等功能模块拆分，避免继续把逻辑堆在单个大文件里。
- 字典内容保留原样，但字典读写能力逐步迁移到 V2 仓储。
- MinIO 作为唯一文件持久化入口，历史本地文件处理代码逐步清理。
- 日志继续使用中文；API 字段、SSE 事件名、数据库字段名保持英文协议。
- 本次不提交代码，所有改动留在本地，方便继续调整。

## 2. 本轮已完成整改

### 2.1 后端 V2 骨架

已新增 `app_v2` 结构：

- `app_v2/api/routes/*`：V2 路由入口。
- `app_v2/application/*`：应用服务层。
- `app_v2/application/training/*`：销售训练应用服务拆分入口。
- `app_v2/infrastructure/repositories/*`：仓储层。
- `app_v2/infrastructure/adapters/*`：文件存储、向量库适配器。
- `app_v2/shared/pagination.py`：分页和 LIKE 查询辅助。

`api/main.py` 已挂载 `/api/v2` 路由。

### 2.2 字典仓储迁移

已新增 `DictionaryRepository`：

- 文件：`app_v2/infrastructure/repositories/dictionary_repository.py`
- 负责直接访问 `dictionary_items` 表。
- 支持列表查询、新增/修改、删除、编码归一化、默认值、metadata 查询。
- 保留 Redis 缓存读写和写后清缓存。
- V2 字典服务不再直接通过旧 `KnowledgeStore` 查询字典。

已迁移到 V2 字典仓储的场景：

- V2 字典管理接口。
- 销售训练画像字典。
- 知识资产文档响应中的 `document_type`、`split_strategy` 字典快照。
- 首页驾驶舱知识库概览中的文档字典快照。

### 2.3 文档仓储迁移

已新增 `DocumentRepository`：

- 文件：`app_v2/infrastructure/repositories/document_repository.py`
- 负责直接访问 `documents` 表。
- 已覆盖列表、详情、MD5 去重、创建、状态更新、软删除/物理删除等能力。

已迁移的调用点：

- 知识库文件列表。
- 知识库文件详情。
- 上传去重。
- 确认入库的 documents 创建。
- 单文件重建索引状态更新。
- 全量重建索引文档读取。
- 文件资产删除链路中的 documents 查询和删除入口。

### 2.4 会话仓储迁移

已新增 `ConversationRepository`：

- 文件：`app_v2/infrastructure/repositories/conversation_repository.py`
- 覆盖聊天记录列表、详情、消息列表、删除。

V2 聊天记录查询和删除已经走仓储层；但聊天生成链路仍复用旧服务，见尾巴清单。

### 2.5 知识资产服务整理

文件：`app_v2/application/knowledge_service.py`

已完成：

- 上传预览、确认入库、文件列表、文件详情、文件预览、删除、单文件重建、全量重建统一收口到 `KnowledgeApplicationService`。
- 文档读写逐步切到 `DocumentRepository`。
- 文档响应的字典归一化改走 V2 `DictionaryRepository`。
- `KnowledgeApplicationService` 内部增加文档字典快照，避免 `_document_to_response` 无快照时回退旧 `KnowledgeStore`。

仍未完成：

- 上传预览、文件提升、索引构建、重载 data 目录仍复用旧 `api.services.upload_services` 和 `api.services.indexing_services`。

### 2.6 首页驾驶舱整改

文件：`app_v2/application/dashboard_service.py`

已完成：

- 首页数据聚合统一收口到 `DashboardApplicationService`。
- 健康检查、知识库概览、训练资料、训练方案、训练会话、最近聊天记录在一个服务中聚合。
- 首页知识库概览的文档字典快照已改走 V2 `DictionaryRepository`。

仍未完成：

- 首页训练资料、训练方案、训练会话的响应转换仍复用 `SalesTrainingService` 里的私有转换方法。

### 2.7 销售训练 V2 外观

已建立以下 V2 应用服务：

- `material_service.py`
- `profile_service.py`
- `goal_service.py`
- `plan_service.py`
- `session_service.py`
- `scoring_service.py`
- `prompt_builders.py`
- `service_provider.py`

已完成：

- 销售训练路由已切入 `/api/v2/training/*`。
- 销售训练画像字典读取已切到 V2 `DictionaryRepository`。
- 训练资料、画像、目标、方案、会话、评分已有 V2 应用服务入口。

仍未完成：

- 真实训练业务逻辑大量仍在旧 `training.services.sales_training_service.SalesTrainingService` 里。
- V2 服务目前更像“外观层”，还没有完全拆出纯 V2 实现。

### 2.8 前端模块化整改

前端项目：`C:\Users\Administrator\WebstormProjects\AI_RAG_Agent_Frontend`

已完成：

- `src/api.ts` 改为兼容导出口。
- API 拆到 `src/shared/api/*`。
- `src/App.vue` 简化为登录、主题和页面编排。
- 应用壳拆到：
  - `src/app/LoginGate.vue`
  - `src/app/AppShell.vue`
  - `src/app/navigation.ts`
- 主要页面拆到：
  - `src/features/dashboard/HomePage.vue`
  - `src/features/chat/ChatPage.vue`
  - `src/features/sales-training/SalesTrainingPage.vue`
  - `src/features/exam/ExamPage.vue`
- 页面使用动态导入，构建产物已有独立页面 chunk。
- `vite.config.ts` 取消 `/api` 前缀剥离，前端统一请求 `/api/v2`。

仍未完成：

- 销售训练页面仍需要继续拆更细组件。
- 知识库、字典、聊天、训练页面的视觉统一和可维护性还需要继续做第二轮治理。

## 3. 当前仍剩的大尾巴

### 尾巴 1：聊天生成链路还在旧服务里

相关文件：

- `app_v2/application/chat_service.py`
- `api/services/chat_services.py`
- `rag/services/rag_service.py`
- `rag/knowledge_store.py`

现状：

- 聊天记录列表、详情、删除已走 V2 `ConversationRepository`。
- 但一次性回答、流式回答、直连 RAG、Agent 工具链、消息保存仍复用旧 `api.services.chat_services`。
- `ChatApplicationService` 仍通过 `_get_knowledge_store()` 获取旧 `KnowledgeStore`。

风险：

- 现在不能删除 `api.services.chat_services`。
- 现在不能删除 `rag.knowledge_store`。
- RAG 链路、Agent 链路、流式链路仍混在旧函数中，不利于测试和扩展。

建议后续动作：

1. 新建 `app_v2/application/rag_service.py`。
2. 新建 `app_v2/application/chat_generation_service.py`。
3. 把 `_should_use_direct_rag`、`_stream_direct_rag`、`_stream_agent` 拆成策略类。
4. 把 `_prepare_chat_conversation`、`_save_chat_exchange` 改为调用 `ConversationRepository`。
5. 删除 V2 对 `api.services.chat_services` 的依赖后，再处理旧兼容模块。

建议使用设计模式：

- 策略模式：区分直连 RAG、Agent 工具链、后续可能的兜底模型。
- 外观模式：保留 `ChatApplicationService` 作为路由入口。
- 工厂方法模式：创建不同模型、向量检索器和回答链路。

### 尾巴 2：知识上传和索引链路仍复用旧服务

相关文件：

- `app_v2/application/knowledge_service.py`
- `api/services/upload_services.py`
- `api/services/indexing_services.py`
- `api/services/document_asset_service.py`
- `api/services/upload_preview_state.py`

现状：

- V2 `KnowledgeApplicationService` 已统一入口。
- documents 表读写已基本迁移到 `DocumentRepository`。
- 但上传预览、临时文件保存、MinIO 对象提升、模型推荐切分、索引构建、data 目录重载仍复用旧服务函数。

风险：

- 旧上传服务不能删。
- 旧索引服务不能删。
- MinIO 统一入口虽然已推进，但文件处理能力还没有完全收口到 V2 adapter/service。

建议后续动作：

1. 新建 `app_v2/application/knowledge/upload_preview_service.py`。
2. 新建 `app_v2/application/knowledge/indexing_service.py`。
3. 新建 `app_v2/application/knowledge/document_asset_service.py`。
4. 把 `_save_preview_file`、`_get_preview_file`、`_promote_preview_file`、`_delete_preview_file` 迁到 V2。
5. 把 `_index_document` 迁到 V2，并让它依赖 `DocumentRepository`、`FileStorageAdapter`、`VectorStoreAdapter`。
6. 所有入口稳定后删除旧 `api.services.upload_services`、`api.services.indexing_services` 的 V2 依赖。

建议使用设计模式：

- 适配器模式：MinIO、本地临时对象、Qdrant 向量库。
- 策略模式：不同切分策略，如 QA、编号段落、递归切分。
- 模板方法模式：统一“读文件 -> 切片 -> 入向量库 -> 更新 documents 状态”的索引流程。

### 尾巴 3：销售训练核心仍是旧大类

相关文件：

- `training/services/sales_training_service.py`
- `app_v2/application/training/*.py`
- `app_v2/infrastructure/repositories/training_repository.py`

现状：

- V2 已有训练资料、画像、目标、方案、会话、评分服务入口。
- 但多数真实业务仍委托 `SalesTrainingService`。
- 首页也还复用 `SalesTrainingService` 的响应转换私有方法。

风险：

- `SalesTrainingService` 是当前最大的业务复杂度集中点。
- 继续叠功能会让销售训练难维护。
- 训练资料、画像生成、目标生成、会话、评分互相耦合，不利于后续加流程式训练、语音训练、人工复核。

建议后续动作：

1. `material_service.py` 接管训练资料上传、预览、发布、删除、重复校验、版本。
2. `profile_service.py` 接管客户画像、学员画像、角色生成、场景润色。
3. `goal_service.py` 接管开放式训练目标、动态轮数、目标达成/失败条件。
4. `session_service.py` 接管训练会话、每轮检索、流式/一次性回答。
5. `scoring_service.py` 接管 40 分通用评分、60 分 LLM 阶段评分、最终报告。
6. 把 `SalesTrainingService` 私有 mapper 移到 V2 独立 mapper 或 schema helper。
7. 确认 V2 覆盖后，删除旧训练服务里的重复实现。

建议使用设计模式：

- 建造者模式：生成训练角色、训练目标、评分规则。
- 策略模式：开放式训练、流程式训练；流式回答、一次性回答。
- 状态模式：训练资料状态、训练会话状态、训练阶段状态。
- 责任链模式：学员回答分析、客户追问生成、风险提醒、评分证据收集。

### 尾巴 4：问答考试仍直接挂旧路由

相关文件：

- `app_v2/api/routes/exam.py`
- `api/routers/exam.py`
- `rag/knowledge_store.py`

现状：

- V2 的 `exam.py` 只是 `from api.routers.exam import router`。
- 旧考试路由仍直接使用旧 `KnowledgeStore`。

风险：

- V2 OpenAPI 看起来有考试接口，但实际实现没有 V2 化。
- 后续删除旧 `api.routers.exam` 会直接断考试。

建议后续动作：

1. 新建 `app_v2/application/exam_service.py`。
2. 新建 `app_v2/infrastructure/repositories/exam_repository.py`。
3. 把考试 session、题目生成、答题、评分从旧路由拆出。
4. 把 Qdrant 候选题检索封装成 V2 adapter。
5. V2 路由不再直接挂旧 `api.routers.exam`。

建议使用设计模式：

- 策略模式：单选、多选、判断、简答、填空生成和评分。
- 工厂方法模式：按题型创建题目生成器。
- 模板方法模式：统一“抽题 -> 生成正式题 -> 答题 -> 评分”的流程。

### 尾巴 5：旧路由和兼容壳仍在

相关文件：

- `api/routers/knowledge.py`
- `api/routers/chat.py`
- `api/routers/dictionaries.py`
- `api/routers/health.py`
- `training/api/router.py`
- `api/chat_services.py`
- `api/upload_services.py`
- `api/indexing_services.py`
- `api/common_services.py`

现状：

- V2 路由已建立。
- 旧路由和兼容壳还没有全部删。
- 一部分旧路由仍包含真实业务，不只是兼容转发。

风险：

- 新旧入口并存，排查问题时容易误判“当前到底走哪个接口”。
- 后续如果前端全部切 V2，旧入口就应该逐步冻结或删除。

建议后续动作：

1. 先确认前端只调用 `/api/v2/*`。
2. 对旧接口做一次 OpenAPI 和前端引用扫描。
3. 对纯兼容壳直接删除。
4. 对仍有真实逻辑的旧路由，先迁移逻辑，再删除。

### 尾巴 6：`KnowledgeStore` 还不能删

相关文件：

- `rag/knowledge_store.py`
- `api/services/common_services.py`
- `api/services/chat_services.py`
- `api/services/upload_services.py`
- `api/services/indexing_services.py`
- `training/services/sales_training_service.py`
- `api/routers/exam.py`

现状：

- 字典、文档、会话已经拆出一部分仓储。
- 但旧服务仍依赖 `KnowledgeStore` 的多个方法。

风险：

- 现在删除 `KnowledgeStore` 会导致聊天、考试、上传、训练等多处断裂。

建议后续动作：

1. 统计 `KnowledgeStore` 方法使用点。
2. 按业务域迁移到对应 V2 repository。
3. 所有调用点清空后，再删除 `rag/knowledge_store.py`。

## 4. 当前小尾巴

### 小尾巴 1：注释还没有全项目补齐

已完成：

- V2 新增服务、仓储、关键测试有较多中文注释。
- 关键迁移原因已有说明。

未完成：

- 旧考试路由、旧销售训练大类、旧聊天服务、旧上传服务仍有大量逻辑缺少适合 Python 初学者阅读的注释。

建议：

- 后续迁移旧代码时同步补注释，不建议单独给即将删除的旧代码大量补注释。
- 对最终保留的 V2 服务、仓储、适配器补“为什么这么做”的注释。

### 小尾巴 2：执行记录文档终端显示乱码

现象：

- 部分中文 Markdown 在 PowerShell 输出中显示乱码。
- 文件本身使用 UTF-8 写入，编辑器中一般能正常显示。

建议：

- 后续文档继续使用 UTF-8。
- 如需命令行查看，可先调整 PowerShell 编码或用编辑器查看。

### 小尾巴 3：前端视觉和组件还需要继续细拆

已完成：

- 页面迁到 `src/features/*`。
- API 拆到 `src/shared/api/*`。
- 首页、聊天、销售训练、考试已动态导入。

未完成：

- 销售训练页面仍偏重。
- 字典、知识库、聊天记录页面还可以继续抽公共表格、分页、搜索、弹窗、确认框组件。

## 5. 建议后续执行顺序

建议不要直接删旧代码。后续按以下顺序做，风险最低：

1. **先拆销售训练大类**
   - 训练业务最复杂，也是后续功能最多的地方。
   - 拆完后页面和接口都会清爽很多。

2. **再拆知识上传和索引**
   - 统一 MinIO、Qdrant、documents 表写入。
   - 这是删除旧上传服务和索引服务的前提。

3. **再拆聊天 RAG 链路**
   - 把直连 RAG、Agent、流式输出、保存消息拆成清晰服务。
   - 这一步完成后 `api.services.chat_services` 才能删。

4. **再拆考试模块**
   - 当前只是 V2 入口挂旧路由。
   - 拆成 V2 服务后，旧 `api.routers.exam` 可以删除。

5. **最后做旧路由、旧服务、旧 KnowledgeStore 清理**
   - 删除前先跑引用扫描。
   - 删除后跑后端全量测试和前端构建。

## 6. 每一步建议测试命令

后端目标测试：

```powershell
.\.venv312\Scripts\python.exe -m pytest tests\test_v2_legacy_dependency_guards.py tests\test_v2_training_no_legacy_service.py tests\test_v2_training_profile_service.py tests\test_v2_dashboard_service.py tests\test_v2_knowledge_service.py tests\test_document_asset_service.py tests\test_v2_chat_service.py tests\test_api_app.py tests\test_auth_api.py -q
```

后端全量测试：

```powershell
.\.venv312\Scripts\python.exe -m pytest -q
```

前端契约测试：

```powershell
node scripts/api-url-contract.test.mjs
node scripts/app-shell-contract.test.mjs
node scripts/feature-pages-contract.test.mjs
node scripts/async-pages-contract.test.mjs
```

前端构建：

```powershell
npm run build
```

旧依赖扫描：

```powershell
rg -n "from api\.services\.chat_services|from api\.services\.upload_services|from api\.services\.indexing_services|from api\.services\.document_asset_service|from api\.routers\.exam import router|from training\.services\.sales_training_service import SalesTrainingService" app_v2
```

## 7. 当前验证结果

最近一次后端目标测试结果：

```text
39 passed, 19 warnings
```

最近一次后端全量测试结果：

```text
115 passed, 1 skipped, 105 warnings
```

warnings 主要是既有问题：

- `langchain-community` deprecation。
- `datetime.utcnow` deprecation。
- FastAPI / Starlette TestClient deprecation。

这些 warning 暂不影响本轮 V2 重构行为，但后续最好集中处理。

前端最近一次验证结果：

- `node scripts/api-url-contract.test.mjs`：通过。
- `node scripts/app-shell-contract.test.mjs`：通过。
- `node scripts/feature-pages-contract.test.mjs`：通过。
- `node scripts/async-pages-contract.test.mjs`：通过。
- `npm run build`：通过，仍有第三方 `@vueuse/core` pure 注释 warning 和 chunk 大于 500KB warning。

## 8. 后续完成标准

可以认为本次 V2 重构真正完成，需要满足：

- `app_v2` 不再导入旧 `api.services.chat_services`、`api.services.upload_services`、`api.services.indexing_services`。
- `app_v2/api/routes/exam.py` 不再直接挂旧 `api.routers.exam`。
- 销售训练 V2 服务不再委托 `training.services.sales_training_service` 的核心业务逻辑。
- 前端只调用 `/api/v2/*`。
- 旧路由没有前端引用，没有 OpenAPI 必要暴露。
- `rag/knowledge_store.py` 不再被生产代码依赖。
- 认证服务、上传预览状态等仍在 `api.services` 下的基础能力已迁移或明确冻结。
- 后端全量测试通过。
- 前端契约测试和构建通过。

## 9. 当前结论

当前处于“大重构中段偏后”：

- V2 骨架已经建立。
- 字典、文档、会话仓储已经拆出。
- 首页、知识资产、销售训练画像已开始摆脱旧 `KnowledgeStore`。
- 但聊天、上传索引、销售训练核心、考试模块仍有明显旧依赖。

粗略估计：

- 架构骨架：已完成约 80%。
- 后端业务迁移：已完成约 55% 到 65%。
- 旧代码清理：已完成约 25% 到 35%。
- 前端模块化：已完成约 60% 到 70%。

下一轮建议从“销售训练大类拆分”开始。

## 10. 全量 V2 化收口状态

> 本节记录的是删除旧文件前的阶段性状态。2026-06-27 完成旧代码物理退场后，当前准确信息以第 11 节为准。

### 已迁移

- 销售训练核心入口：已迁移到 `app_v2/application/training/*`，V2 小服务构造阶段不再创建旧 `SalesTrainingService`。
- 知识上传和索引入口：已迁移到 `app_v2/application/knowledge/*`，V2 knowledge 服务不再直接导入旧上传、旧索引和旧文件资产业务服务。
- 聊天生成入口：已迁移到 `app_v2/application/chat_generation_service.py`。
- 考试入口：已迁移到 `app_v2/application/exam_service.py`，V2 exam 路由不再直接挂旧 `api.routers.exam`。
- 文档响应转换：已迁移到 `app_v2/shared/document_response.py`。
- 过渡期 `KnowledgeStore` 创建入口：已迁移到 `app_v2/infrastructure/repositories/knowledge_store_provider.py`，避免 V2 继续从旧 `api.services.common_services` 取入口；`app_v2` 内除该 provider 外不再直接 import 或实例化 `KnowledgeStore`。

### 冻结旧实现

- `api/services/chat_services.py`、`api/services/upload_services.py`、`api/services/indexing_services.py`、`api/services/document_asset_service.py`：不再作为 `app_v2` 直接导入入口，后续只允许作为历史对照或旧接口兼容，不允许新增业务逻辑。
- `api/routers/exam.py`：V2 不再直接挂载；旧路由如仍保留，只能作为旧入口兼容。
- `training/services/sales_training_service.py`：V2 入口已切出，旧大类不再承接新增业务。
- `rag/knowledge_store.py`：仍是底层过渡依赖，不能直接删除；后续按业务域继续迁移方法到 V2 repository。
- `api.services.auth_services` 和 `api.services.upload_preview_state`：当前仍被 V2 认证和上传预览链路复用，归类为基础能力兼容项，后续如要彻底清理 `api.services` 包，需要单独迁移。

### 待删除

- 纯兼容壳文件和旧路由需要在确认无前端引用、OpenAPI 不再暴露旧路径、目标测试和全量测试通过后再删除。
- 删除旧文件前必须跑严格旧依赖扫描：

```powershell
rg -n "from api\.services\.chat_services|from api\.services\.upload_services|from api\.services\.indexing_services|from api\.services\.document_asset_service|from api\.routers\.exam import router|from training\.services\.sales_training_service import SalesTrainingService" app_v2
```

### 当前验收结果

- 严格旧依赖扫描：无匹配。
- 后端目标测试：`60 passed, 26 warnings`。
- 后端全量测试：`136 passed, 1 skipped, 119 warnings`。
- 前端契约测试：四个脚本全部通过。
- 前端构建：通过，保留既有第三方 warning 和大 chunk warning。

## 11. 最终尾项清理状态

> 本节是 2026-06-27 最终收口后的准确信息；前文早期“仍未完成”段落保留为历史复盘，不再作为当前状态判断依据。

### 已完成收口

- 旧 `api/routers/*`、`api/services/*`、顶层兼容壳 `api/chat_services.py`、`api/common_services.py`、`api/indexing_services.py`、`api/upload_services.py` 已物理删除。
- 旧 `training/services/sales_training_service.py`、`training/api/router.py`、`rag/knowledge_store.py` 和 `app_v2/infrastructure/repositories/knowledge_store_provider.py` 已物理删除。
- 默认字典种子迁入 `app_v2/infrastructure/repositories/default_dictionaries.py`，字典初始化由 `DictionaryRepository.seed_default_items()` 承接。
- documents 历史存储字段补齐迁入 `DocumentRepository.ensure_storage_columns()`，启动预热通过 `bootstrap_v2_metadata()` 显式初始化。
- 考试持久化已由 `app_v2/infrastructure/repositories/exam_repository.py` 直接走 ORM，不再通过旧 `KnowledgeStore`。
- 内部任务路由迁入 `app_v2/api/routes/internal_jobs.py`，URL 仍保持 `/internal/jobs/minio/cleanup-preview-uploads`，避免 XXL-JOB 配置立刻变更。
- 模型档位归一化改为 `DictionaryRepository.normalize_code("model_mode", ...)`，不再创建旧存储对象。

### 删除后守卫

- `tests/test_v2_legacy_dependency_guards.py` 已改为物理退场守卫，要求以下路径不存在：
  - `api/services`
  - `api/routers`
  - `training/services/sales_training_service.py`
  - `rag/knowledge_store.py`
  - `app_v2/infrastructure/repositories/knowledge_store_provider.py`
- 严格扫描已确认生产和测试代码无旧导入：

```powershell
rg -n "from api\.services|import api\.services|from api\.routers|import api\.routers|from training\.services\.sales_training_service|import training\.services\.sales_training_service|from rag\.knowledge_store|import rag\.knowledge_store|KnowledgeStore\(" api app_v2 model rag scripts tests training --glob "*.py"
```

### 当前验收结果

- V2 系统管理目标测试：`12 passed, 11 warnings`。
- 后端全量测试：`126 passed, 1 skipped, 104 warnings`。
- 严格旧依赖扫描：排除守卫测试自身禁用字符串后，无旧导入匹配。
- 旧路径存在性检查：`api/services`、`api/routers`、`training/services`、`training/services/sales_training_service.py`、`rag/knowledge_store.py`、`app_v2/infrastructure/repositories/knowledge_store_provider.py` 均为 `False`。
- 前端契约测试：`api-url-contract`、`app-shell-contract`、`feature-pages-contract`、`async-pages-contract` 全部通过。
- 前端构建：`npm run build` 通过，仍保留既有 `@vueuse/core` pure 注释 warning 和 chunk 大于 500KB warning。

### 回归中补齐项

- 删除旧代码后的全量回归暴露了 V2 系统管理菜单写接口的半完成状态：`SystemMenuCreateRequest`、`SystemMenuUpdateRequest`、菜单仓储已存在，但应用服务和路由未完整承接。
- 已补齐 `SystemApplicationService.create_menu()`、`update_menu()`、`disable_menu()`、`enable_menu()`，并在 `app_v2/api/routes/system.py` 暴露 `/api/v2/system/menus/{menu_id}` 和 `/api/v2/system/menus/{menu_id}/enable`。
- 角色菜单保存改为只解析雪花 ID 并覆盖关系，不再依赖启用菜单列表做强校验，避免菜单授权编辑被当前可见菜单状态误伤。

### 剩余注意

- 工作树里仍有一些与本轮无关的历史 docs 删除记录，提交前必须分拣，不要把无关删除混入同一次提交。
- 前端大 chunk warning 仍属于后续前端拆分优化事项，不影响本轮旧后端代码退场。
