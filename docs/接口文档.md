# 接口文档

本文档由 `scripts/generate_api_docs.py` 根据当前 FastAPI OpenAPI 元数据生成，
并补充了流式 SSE 事件说明和 Pydantic 模型中的中文字段注释。

## 基础说明

| 项目 | 内容 |
| --- | --- |
| OpenAPI 标题 | 知习台 API |
| 版本 | 1.0.0 |
| 描述 | 知习台后端服务，提供知识库问答、资料管理、销售陪练和掌握度测评能力。 |
| 应用入口 | `api/main.py` |
| 默认本地地址 | `http://127.0.0.1:8000` 或 `http://localhost:8000` |
| 路由前缀 | 当前没有统一 `/api` 前缀，训练模块单独使用 `/training` 前缀 |
| 普通响应 | JSON |
| 流式响应 | SSE 文本流，`Content-Type: text/event-stream` |
| 在线文档 | `/docs` Swagger UI，`/redoc` ReDoc，`/openapi.json` 原始 OpenAPI |

## 通用约定

- `model_mode` 当前使用字典项：`high`、`medium`、`low`。
- `collection_name` 为空时，后端使用配置中的默认 Qdrant collection。
- 普通接口校验失败会返回 FastAPI 标准 `422` 响应。
- 本项目当前没有统一鉴权中间件，接口文档不包含登录态或 token 说明。
- 知识正文、向量和训练切片正文以 Qdrant 为准，MySQL 只保存业务元数据和状态。

## 接口总览

### 健康检查

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/health` | 查询服务健康状态 |

### 聊天和会话

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/conversations` | 分页查询聊天会话 |
| `GET` | `/conversations/{conversation_id}` | 查询聊天会话详情 |
| `DELETE` | `/conversations/{conversation_id}` | 删除聊天会话 |
| `POST` | `/chat` | 一次性聊天回答 |
| `POST` | `/chat/stream` | 流式聊天回答 |
| `POST` | `/debug/retrieve` | 调试 RAG 检索结果 |

### 知识库文件管理

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/knowledge/upload/preview` | 上传知识库文件并预览识别结果 |
| `POST` | `/knowledge/upload/recommend` | 让模型推荐文档类型和切分策略 |
| `POST` | `/knowledge/upload/confirm` | 确认上传并正式入库 |
| `GET` | `/knowledge/files` | 查询知识库文件列表 |
| `GET` | `/knowledge/files/{document_id}` | 查询知识库文件详情 |
| `DELETE` | `/knowledge/files/{document_id}` | 删除知识库文件 |
| `GET` | `/knowledge/files/{document_id}/preview` | 预览已入库文件原文 |
| `POST` | `/knowledge/files/reindex-all` | 全量重建知识库索引 |
| `POST` | `/knowledge/files/{document_id}/reindex` | 重建单个文件索引 |
| `POST` | `/knowledge/reload` | 扫描 data 目录并重载知识库 |

### 系统字典

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/dictionaries` | 查询系统字典 |
| `POST` | `/dictionaries/items` | 新增或更新字典项 |

### 知识掌握度测评

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/exam/sections` | 查询考试题源目录 |
| `POST` | `/exam/sessions` | 开始一场知识测评 |
| `GET` | `/exam/sessions` | 分页查询测评历史 |
| `POST` | `/exam/sessions/{session_id}/answer` | 提交当前轮答案 |
| `GET` | `/exam/sessions/{session_id}` | 查询测评详情 |

### 销售训练

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/training/profile-dictionaries` | 查询销售训练画像字典 |
| `POST` | `/training/knowledge/upload` | 上传销售训练资料并生成待发布切片 |
| `GET` | `/training/knowledge/batches` | 分页查询训练资料批次 |
| `GET` | `/training/knowledge/batches/{batch_id}/preview` | 预览训练资料原文 |
| `DELETE` | `/training/knowledge/batches/{batch_id}` | 删除训练资料批次 |
| `POST` | `/training/knowledge/batches/{batch_id}/publish` | 发布训练资料批次 |
| `POST` | `/training/knowledge/batches/{batch_id}/rollback` | 回滚训练资料版本 |
| `POST` | `/training/knowledge/batches/{batch_id}/reparse` | 重新切分训练资料批次 |
| `GET` | `/training/knowledge/batches/{batch_id}/versions` | 查询训练资料版本链 |
| `GET` | `/training/knowledge/batches/{batch_id}/chunks` | 查询训练资料切片预览 |
| `POST` | `/training/plans` | 创建销售训练方案 |
| `GET` | `/training/plans` | 分页查询销售训练方案 |
| `GET` | `/training/plans/{plan_id}` | 查询销售训练方案详情 |
| `PUT` | `/training/plans/{plan_id}` | 更新销售训练方案 |
| `POST` | `/training/profiles/generate` | 生成 AI 客户角色 |
| `POST` | `/training/profiles/scenario/polish` | 润色训练场景描述 |
| `POST` | `/training/profiles/supplement-questions/generate` | 生成角色补充问答题 |
| `POST` | `/training/profiles/{profile_id}/goal-settings/generate` | 生成训练目标和评分设置 |
| `POST` | `/training/sessions` | 开始销售训练会话 |
| `GET` | `/training/sessions` | 分页查询销售训练会话 |
| `GET` | `/training/sessions/{session_id}` | 查询销售训练复盘详情 |
| `POST` | `/training/sessions/{session_id}/turns` | 提交学员回复 |
| `POST` | `/training/sessions/{session_id}/final-score` | 生成最终训练评分 |

## 接口详情

### 健康检查

#### GET `/health`

- 说明：查询服务健康状态
- Operation ID：`health_health_get`

请求参数：无 path/query 参数。

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`HealthResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `status` | `string` | 是 |  | 整体状态；ok 表示全部可用，degraded 表示部分依赖不可用 |
| `qdrant` | `string` | 是 |  | Qdrant 状态；ok 或 unavailable |
| `redis` | `string` | 否 | `unavailable` | Redis 状态；ok 或 unavailable |
| `collection_name` | `string` | 是 |  | 当前项目使用的 Qdrant collection 名称 |
| `collections` | `array[string]` | 否 |  | Qdrant 中已有的 collection 列表 |
| `collection_points` | `object` | 否 |  | 每个 collection 在 Qdrant 中的向量点数量 |

### 聊天和会话

#### GET `/conversations`

- 说明：分页查询聊天会话
- 详细说明：分页查询聊天记录列表。
- Operation ID：`list_conversations_conversations_get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `page` | `query` | 否 | `integer` | `1` | Page |
| `page_size` | `query` | 否 | `integer` | `10` | Page Size |
| `user_id` | `query` | 否 | `string | null` |  | User Id |
| `keyword` | `query` | 否 | `string | null` |  | Keyword |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`ConversationListResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `items` | `array[ConversationSummaryResponse]` | 是 |  | 当前页会话摘要 |
| `total` | `integer` | 是 |  | 总会话数 |
| `page` | `integer` | 是 |  | 当前页码 |
| `page_size` | `integer` | 是 |  | 每页数量 |

#### GET `/conversations/{conversation_id}`

- 说明：查询聊天会话详情
- 详细说明：查询单个聊天记录详情。
- Operation ID：`get_conversation_detail_conversations__conversation_id__get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `conversation_id` | `path` | 是 | `string` |  | Conversation Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`ConversationDetailResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `conversation` | `ConversationSummaryResponse` | 是 |  | 会话摘要；对象模型：`ConversationSummaryResponse` |
| `messages` | `array[ConversationMessageResponse]` | 是 |  | 会话全部消息 |

#### DELETE `/conversations/{conversation_id}`

- 说明：删除聊天会话
- 详细说明：删除聊天记录。
- Operation ID：`delete_conversation_conversations__conversation_id__delete`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `conversation_id` | `path` | 是 | `string` |  | Conversation Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`ConversationDeleteResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `status` | `string` | 是 |  | 固定返回 deleted |
| `conversation_id` | `string` | 是 |  | 被删除的会话 ID |

#### POST `/chat`

- 说明：一次性聊天回答
- 详细说明：一次性聊天接口。
- Operation ID：`chat_chat_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`application/json`
- Schema：`ChatRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `message` | `string` | 是 |  | 用户输入的问题；不能为空字符串 |
| `user_id` | `string | null` | 否 |  | 当前会话用户 ID；可为空，工具层会兜底随机用户 |
| `conversation_id` | `string | null` | 否 |  | 当前会话 ID；为空时后端创建新会话 |
| `model_mode` | `string | null` | 否 |  | 回答模型档位，具体可选值和默认值来自 model_mode 字典 |
| `collection_name` | `string | null` | 否 |  | 本次聊天要检索的 Qdrant collection；为空时使用默认 collection |

响应体：

- Content-Type：`application/json`
- Schema：`ChatResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `answer` | `string` | 是 |  | 一次性接口返回的完整最终回答 |
| `conversation_id` | `string` | 是 |  | 当前会话 ID；前端后续请求需要继续携带 |
| `first_token_ms` | `number | null` | 否 |  | 首字/首片耗时；一次性接口没有真实首字，默认等于 total_ms |
| `total_ms` | `number | null` | 否 |  | 本次请求总耗时 |

#### POST `/chat/stream`

- 说明：流式聊天回答
- 详细说明：流式聊天接口。
- Operation ID：`chat_stream_chat_stream_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`application/json`
- Schema：`ChatRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `message` | `string` | 是 |  | 用户输入的问题；不能为空字符串 |
| `user_id` | `string | null` | 否 |  | 当前会话用户 ID；可为空，工具层会兜底随机用户 |
| `conversation_id` | `string | null` | 否 |  | 当前会话 ID；为空时后端创建新会话 |
| `model_mode` | `string | null` | 否 |  | 回答模型档位，具体可选值和默认值来自 model_mode 字典 |
| `collection_name` | `string | null` | 否 |  | 本次聊天要检索的 Qdrant collection；为空时使用默认 collection |

响应体：SSE 文本流，事件详见下方“流式事件”。

流式事件：

| 事件名 | 说明 |
| --- | --- |
| `meta` | 流开始元信息，包含 conversation_id、模型档位、模型名称和 collection。 |
| `metric` | 首字耗时指标，当前包含 first_token_ms。 |
| `chunk` | 模型回答增量文本，data.content 是本次新增内容。 |
| `done` | 流式回答完成，包含 conversation_id、模型信息、first_token_ms、total_ms。 |
| `error` | 流式处理失败，data.error 是错误信息。 |

#### POST `/debug/retrieve`

- 说明：调试 RAG 检索结果
- 详细说明：调试 RAG 检索链路。
- Operation ID：`debug_retrieve_debug_retrieve_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`application/json`
- Schema：`DebugRetrieveRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `query` | `string` | 是 |  | 要调试的用户问题 |

响应体：

- Content-Type：`application/json`
- Schema：`Response Debug Retrieve Debug Retrieve Post`

类型：`object`。

### 知识库文件管理

#### POST `/knowledge/upload/preview`

- 说明：上传知识库文件并预览识别结果
- 详细说明：上传文件并返回识别结果，等待用户确认后再正式入库。
- Operation ID：`preview_knowledge_file_knowledge_upload_preview_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`multipart/form-data`
- Schema：`Body_preview_knowledge_file_knowledge_upload_preview_post`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `file` | `string` | 是 |  | File |

响应体：

- Content-Type：`application/json`
- Schema：`KnowledgeUploadPreviewResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `upload_id` | `string` | 是 |  | 临时上传 ID，确认入库时使用 |
| `filename` | `string` | 是 |  | 原始文件名 |
| `file_type` | `string` | 是 |  | 文件类型 |
| `file_size` | `integer` | 是 |  | 文件大小 |
| `file_md5` | `string` | 是 |  | 文件 MD5 |
| `duplicate` | `boolean` | 否 | `False` | 是否与已有 active 文件重复 |
| `duplicate_document` | `KnowledgeFileResponse | null` | 否 |  | 重复时对应的已有文件 |
| `detected_type` | `string` | 是 |  | 系统识别的文档类型 |
| `split_strategy` | `string` | 是 |  | 系统建议的切分策略 |
| `confidence` | `number` | 是 |  | 识别置信度 |
| `reasons` | `array[string]` | 否 |  | 识别原因 |
| `llm_used` | `boolean` | 否 | `False` | 是否使用了 LLM 兜底 |
| `sample_text` | `string` | 否 | `` | 抽样文本，给前端预览 |

#### POST `/knowledge/upload/recommend`

- 说明：让模型推荐文档类型和切分策略
- 详细说明：对临时上传文件调用模型推荐文档类型和切分策略。
- Operation ID：`recommend_knowledge_upload_knowledge_upload_recommend_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`application/json`
- Schema：`KnowledgeUploadRecommendRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `upload_id` | `string` | 是 |  | 预览阶段返回的 upload_id |

响应体：

- Content-Type：`application/json`
- Schema：`KnowledgeUploadRecommendResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `document_type` | `string` | 是 |  | 模型推荐的文档类型 |
| `split_strategy` | `string` | 是 |  | 模型推荐的切分策略：numbered_qa/outline_qa/numbered_segments/recursive |
| `confidence` | `number` | 是 |  | 模型推荐置信度，范围 0 到 1 |
| `reasons` | `array[string]` | 否 |  | 模型推荐原因 |
| `sample_chars` | `integer` | 是 |  | 实际发送给模型的样本文本字符数 |
| `model_name` | `string` | 是 |  | 本次用于推荐的模型名称 |

#### POST `/knowledge/upload/confirm`

- 说明：确认上传并正式入库
- 详细说明：确认预览结果，并正式写入 MySQL 业务表和 Qdrant。
- Operation ID：`confirm_knowledge_file_knowledge_upload_confirm_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`application/json`
- Schema：`KnowledgeUploadConfirmRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `upload_id` | `string` | 是 |  | 预览阶段返回的 upload_id |
| `document_type` | `string` | 是 |  | 用户确认后的文档结构类型：qa/numbered/text |
| `split_strategy` | `string` | 是 |  | 用户确认后的切分策略：numbered_qa/outline_qa/numbered_segments/recursive |
| `collection_name` | `string | null` | 否 |  | 用户选择或输入的 Qdrant collection；为空时使用默认 collection |

响应体：

- Content-Type：`application/json`
- Schema：`KnowledgeUploadResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `status` | `string` | 是 |  | indexed 或 duplicate |
| `message` | `string` | 是 |  | 面向调用方的简短说明 |
| `document` | `KnowledgeFileResponse` | 是 |  | 成功入库或已存在的文件记录；对象模型：`KnowledgeFileResponse` |

#### GET `/knowledge/files`

- 说明：查询知识库文件列表
- 详细说明：查询知识库文件列表。
- Operation ID：`list_knowledge_files_knowledge_files_get`

请求参数：无 path/query 参数。

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`array[KnowledgeFileResponse]`

数组元素：`KnowledgeFileResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `document_id` | `string` | 是 |  | 文件唯一 ID；删除和重建索引都靠它定位文件 |
| `filename` | `string` | 是 |  | 用户上传时的原始文件名 |
| `file_path` | `string` | 是 |  | MinIO 存储 URI，格式为 minio://桶名/对象路径 |
| `file_type` | `string` | 是 |  | 文件类型，例如 txt/pdf |
| `file_md5` | `string` | 是 |  | 文件内容 MD5；用于判断重复上传 |
| `file_size` | `integer` | 是 |  | 文件大小，单位字节 |
| `status` | `string` | 是 |  | uploaded/indexing/indexed/failed/deleted |
| `version` | `integer` | 是 |  | 文件索引版本；每次 reindex 会递增 |
| `chunk_count` | `integer` | 是 |  | 当前版本写入 Qdrant 的知识单元数量 |
| `collection_name` | `string` | 否 | `agent` | 文件写入的 Qdrant collection |
| `document_type` | `string` | 否 | `text` | 文档结构类型：qa/numbered/text |
| `split_strategy` | `string` | 否 | `recursive` | 文件入库时使用的切分策略：numbered_qa/outline_qa/numbered_segments/recursive |
| `created_at` | `string` | 是 |  | 文件记录创建时间 |
| `updated_at` | `string` | 是 |  | 文件记录最后更新时间 |
| `error_message` | `string | null` | 否 |  | 入库失败时保存错误原因 |

#### GET `/knowledge/files/{document_id}`

- 说明：查询知识库文件详情
- 详细说明：查询单个知识库文件详情。
- Operation ID：`get_knowledge_file_knowledge_files__document_id__get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `document_id` | `path` | 是 | `string` |  | Document Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`KnowledgeFileResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `document_id` | `string` | 是 |  | 文件唯一 ID；删除和重建索引都靠它定位文件 |
| `filename` | `string` | 是 |  | 用户上传时的原始文件名 |
| `file_path` | `string` | 是 |  | MinIO 存储 URI，格式为 minio://桶名/对象路径 |
| `file_type` | `string` | 是 |  | 文件类型，例如 txt/pdf |
| `file_md5` | `string` | 是 |  | 文件内容 MD5；用于判断重复上传 |
| `file_size` | `integer` | 是 |  | 文件大小，单位字节 |
| `status` | `string` | 是 |  | uploaded/indexing/indexed/failed/deleted |
| `version` | `integer` | 是 |  | 文件索引版本；每次 reindex 会递增 |
| `chunk_count` | `integer` | 是 |  | 当前版本写入 Qdrant 的知识单元数量 |
| `collection_name` | `string` | 否 | `agent` | 文件写入的 Qdrant collection |
| `document_type` | `string` | 否 | `text` | 文档结构类型：qa/numbered/text |
| `split_strategy` | `string` | 否 | `recursive` | 文件入库时使用的切分策略：numbered_qa/outline_qa/numbered_segments/recursive |
| `created_at` | `string` | 是 |  | 文件记录创建时间 |
| `updated_at` | `string` | 是 |  | 文件记录最后更新时间 |
| `error_message` | `string | null` | 否 |  | 入库失败时保存错误原因 |

#### DELETE `/knowledge/files/{document_id}`

- 说明：删除知识库文件
- 详细说明：按 document_id 删除知识库文件。
- Operation ID：`delete_knowledge_file_knowledge_files__document_id__delete`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `document_id` | `path` | 是 | `string` |  | Document Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`KnowledgeDeleteResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `status` | `string` | 是 |  | 固定返回 deleted |
| `document_id` | `string` | 是 |  | 被删除的文件 ID |

#### GET `/knowledge/files/{document_id}/preview`

- 说明：预览已入库文件原文
- 详细说明：预览已入库知识库文件的原始文本内容。
- Operation ID：`preview_indexed_knowledge_file_knowledge_files__document_id__preview_get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `document_id` | `path` | 是 | `string` |  | Document Id |
| `max_chars` | `query` | 否 | `integer` | `20000` | 最多返回的预览字符数，避免大文件一次性返回过多内容。 |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`KnowledgeFilePreviewResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `document` | `KnowledgeFileResponse` | 是 |  | 被预览的文件元数据；对象模型：`KnowledgeFileResponse` |
| `preview_type` | `string` | 是 |  | text/pdf_text/unsupported，表示本次预览内容的来源类型 |
| `content` | `string` | 是 |  | 预览文本内容；大文件会按 max_chars 截断 |
| `truncated` | `boolean` | 是 |  | 是否因为超过 max_chars 被截断 |
| `page_count` | `integer | null` | 否 |  | PDF 页数；TXT 文件为空 |

#### POST `/knowledge/files/reindex-all`

- 说明：全量重建知识库索引
- 详细说明：清空 Qdrant collection，并重建所有 active 知识库文件的索引。
- Operation ID：`reindex_all_knowledge_files_knowledge_files_reindex_all_post`

请求参数：无 path/query 参数。

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`KnowledgeBulkReindexResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `status` | `string` | 是 |  | ok 或 partial_failed |
| `total` | `integer` | 是 |  | 参与重建的文件数 |
| `succeeded` | `integer` | 是 |  | 成功数量 |
| `failed` | `integer` | 是 |  | 失败数量 |
| `results` | `array[KnowledgeReindexResult]` | 是 |  | 每个文件的结果 |

#### POST `/knowledge/files/{document_id}/reindex`

- 说明：重建单个文件索引
- 详细说明：重新解析并索引某个知识库文件。
- Operation ID：`reindex_knowledge_file_knowledge_files__document_id__reindex_post`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `document_id` | `path` | 是 | `string` |  | Document Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`KnowledgeFileResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `document_id` | `string` | 是 |  | 文件唯一 ID；删除和重建索引都靠它定位文件 |
| `filename` | `string` | 是 |  | 用户上传时的原始文件名 |
| `file_path` | `string` | 是 |  | MinIO 存储 URI，格式为 minio://桶名/对象路径 |
| `file_type` | `string` | 是 |  | 文件类型，例如 txt/pdf |
| `file_md5` | `string` | 是 |  | 文件内容 MD5；用于判断重复上传 |
| `file_size` | `integer` | 是 |  | 文件大小，单位字节 |
| `status` | `string` | 是 |  | uploaded/indexing/indexed/failed/deleted |
| `version` | `integer` | 是 |  | 文件索引版本；每次 reindex 会递增 |
| `chunk_count` | `integer` | 是 |  | 当前版本写入 Qdrant 的知识单元数量 |
| `collection_name` | `string` | 否 | `agent` | 文件写入的 Qdrant collection |
| `document_type` | `string` | 否 | `text` | 文档结构类型：qa/numbered/text |
| `split_strategy` | `string` | 否 | `recursive` | 文件入库时使用的切分策略：numbered_qa/outline_qa/numbered_segments/recursive |
| `created_at` | `string` | 是 |  | 文件记录创建时间 |
| `updated_at` | `string` | 是 |  | 文件记录最后更新时间 |
| `error_message` | `string | null` | 否 |  | 入库失败时保存错误原因 |

#### POST `/knowledge/reload`

- 说明：扫描 data 目录并重载知识库
- 详细说明：扫描 data/ 目录并按新结构重建知识库。
- Operation ID：`reload_knowledge_knowledge_reload_post`

请求参数：无 path/query 参数。

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`Response Reload Knowledge Knowledge Reload Post`

类型：`object`。

### 系统字典

#### GET `/dictionaries`

- 说明：查询系统字典
- 详细说明：查询系统字典，返回按字典编码分组后的多层级结构。
- Operation ID：`list_dictionaries_dictionaries_get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `dictionary_code` | `query` | 否 | `string | null` |  | 字典编码；为空时返回全部字典 |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`array[DictionaryGroupResponse]`

数组元素：`DictionaryGroupResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `dictionary_code` | `string` | 是 |  | 字典编码 |
| `dictionary_name` | `string` | 是 |  | 字典名称 |
| `items` | `array[DictionaryItemResponse]` | 否 |  | 当前字典的树形字典项 |

#### POST `/dictionaries/items`

- 说明：新增或更新字典项
- 详细说明：新增或更新字典项。
- Operation ID：`upsert_dictionary_item_dictionaries_items_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`application/json`
- Schema：`DictionaryItemUpsertRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `dictionary_code` | `string` | 是 |  | 字典编码，例如 model_mode |
| `dictionary_name` | `string` | 是 |  | 字典名称，例如 回答模型档位 |
| `item_code` | `string` | 是 |  | 字典项编码，同一字典下唯一 |
| `item_name` | `string` | 是 |  | 字典项展示名称 |
| `parent_item_id` | `string | null` | 否 |  | 父级字典项 ID；为空表示一级项 |
| `parent_item_code` | `string | null` | 否 |  | 父级字典项编码；创建子项时可用编码定位父级 |
| `sort_order` | `integer` | 否 | `0` | 同级排序号 |
| `enabled` | `boolean` | 否 | `True` | 是否启用 |
| `description` | `string | null` | 否 |  | 字典项说明 |
| `metadata` | `object` | 否 |  | 业务扩展元数据，例如 collection_name/keyword |

响应体：

- Content-Type：`application/json`
- Schema：`DictionaryItemResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `dictionary_item_id` | `string` | 是 |  | 字典项唯一 ID |
| `dictionary_code` | `string` | 是 |  | 字典编码，例如 document_structure |
| `dictionary_name` | `string` | 是 |  | 字典名称，例如 文档结构类型 |
| `item_code` | `string` | 是 |  | 字典项编码，例如 qa |
| `item_name` | `string` | 是 |  | 字典项名称，例如 问答型 |
| `parent_item_id` | `string | null` | 否 |  | 父级字典项 ID；为空表示一级项 |
| `item_level` | `integer` | 是 |  | 层级，从 1 开始 |
| `sort_order` | `integer` | 是 |  | 同级排序号 |
| `enabled` | `boolean` | 是 |  | 是否启用 |
| `description` | `string | null` | 否 |  | 字典项说明 |
| `metadata` | `object` | 否 |  | 前端展示或业务扩展元数据 |
| `children` | `array[DictionaryItemResponse]` | 否 |  | 子级字典项 |

### 知识掌握度测评

#### GET `/exam/sections`

- 说明：查询考试题源目录
- 详细说明：查询某个向量库/文件下可用于考试的目录路径。
- Operation ID：`list_exam_sections_exam_sections_get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `collection_name` | `query` | 否 | `string | null` |  | Collection Name |
| `document_id` | `query` | 否 | `string | null` |  | Document Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`ExamSectionsResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `collection_name` | `string` | 是 |  | 向量库名称 |
| `document_id` | `string | null` | 否 |  | 文件编号 |
| `sections` | `array[ExamSectionResponse]` | 否 |  | 目录列表 |

#### POST `/exam/sessions`

- 说明：开始一场知识测评
- 详细说明：开始一场对话式随机考试。
- Operation ID：`start_exam_session_exam_sessions_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`application/json`
- Schema：`ExamStartRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `title` | `string` | 是 |  | 本场测评名称；历史记录中展示 |
| `collection_name` | `string | null` | 否 |  | 题源所在 Qdrant collection；为空时使用默认 collection |
| `document_id` | `string | null` | 否 |  | 指定题源文件；为空时从整个 collection 抽题 |
| `section_path` | `string | null` | 否 |  | 指定目录路径；为空时不按目录过滤 |
| `user_id` | `string | null` | 否 |  | 考试用户编号 |
| `round_count` | `integer` | 否 | `5` | 考试轮数 |
| `question_types` | `array[enum]` | 否 |  | 允许随机生成的题型 |
| `model_mode` | `string | null` | 否 |  | 主观题分析模型档位 |
| `seed` | `integer | null` | 否 |  | 随机种子；传入后同范围抽题更容易复现 |

响应体：

- Content-Type：`application/json`
- Schema：`ExamStartResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `session` | `ExamSessionSummary` | 是 |  | 考试会话摘要；对象模型：`ExamSessionSummary` |
| `current_question` | `ExamConversationQuestion` | 是 |  | 第一轮题目；对象模型：`ExamConversationQuestion` |

#### GET `/exam/sessions`

- 说明：分页查询测评历史
- 详细说明：分页查询考试历史记录。
- Operation ID：`list_exam_sessions_exam_sessions_get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `page` | `query` | 否 | `integer` | `1` | Page |
| `page_size` | `query` | 否 | `integer` | `10` | Page Size |
| `user_id` | `query` | 否 | `string | null` |  | User Id |
| `keyword` | `query` | 否 | `string | null` |  | Keyword |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`ExamHistoryListResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `items` | `array[ExamSessionSummary]` | 是 |  | 当前页考试记录 |
| `total` | `integer` | 是 |  | 总数 |
| `page` | `integer` | 是 |  | 当前页 |
| `page_size` | `integer` | 是 |  | 每页数量 |

#### POST `/exam/sessions/{session_id}/answer`

- 说明：提交当前轮答案
- 详细说明：提交当前轮答案，并返回分析和下一题。
- Operation ID：`answer_exam_session_exam_sessions__session_id__answer_post`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `session_id` | `path` | 是 | `string` |  | Session Id |

请求体：

- Content-Type：`application/json`
- Schema：`ExamAnswerRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `answer` | `string | array[string]` | 否 | `` | 用户答案；多选题可传字符串数组 |

响应体：

- Content-Type：`application/json`
- Schema：`ExamAnswerResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `session` | `ExamSessionSummary` | 是 |  | 更新后的考试会话；对象模型：`ExamSessionSummary` |
| `answered_question` | `ExamConversationQuestion` | 是 |  | 已作答题目；对象模型：`ExamConversationQuestion` |
| `analysis` | `ExamAnswerAnalysis` | 是 |  | 本题分析；对象模型：`ExamAnswerAnalysis` |
| `next_question` | `ExamConversationQuestion | null` | 否 |  | 下一轮题目；考试完成时为空 |

#### GET `/exam/sessions/{session_id}`

- 说明：查询测评详情
- 详细说明：查询考试历史详情，包含题目、用户答案和分析。
- Operation ID：`get_exam_session_detail_exam_sessions__session_id__get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `session_id` | `path` | 是 | `string` |  | Session Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`ExamSessionDetailResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `session` | `ExamSessionSummary` | 是 |  | 考试会话摘要；对象模型：`ExamSessionSummary` |
| `questions` | `array[ExamQuestionRecord]` | 是 |  | 全部题目和用户答案 |

### 销售训练

#### GET `/training/profile-dictionaries`

- 说明：查询销售训练画像字典
- 详细说明：查询销售训练画像字典。
- Operation ID：`list_profile_dictionaries_training_profile_dictionaries_get`

请求参数：无 path/query 参数。

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`array[DictionaryGroupResponse]`

数组元素：`DictionaryGroupResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `dictionary_code` | `string` | 是 |  | 字典编码 |
| `dictionary_name` | `string` | 是 |  | 字典名称 |
| `items` | `array[DictionaryItemResponse]` | 否 |  | 当前字典的树形字典项 |

#### POST `/training/knowledge/upload`

- 说明：上传销售训练资料并生成待发布切片
- 详细说明：上传销售训练知识，并生成待确认发布的预览切片。
- Operation ID：`upload_training_knowledge_training_knowledge_upload_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`multipart/form-data`
- Schema：`Body_upload_training_knowledge_training_knowledge_upload_post`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `file` | `string` | 是 |  | File |
| `source_type` | `string` | 否 | `lms_case` | Source Type |
| `model_mode` | `string | null` | 否 |  | Model Mode |
| `created_by` | `string | null` | 否 |  | Created By |

响应体：

- Content-Type：`application/json`
- Schema：`TrainingKnowledgeUploadResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `batch_id` | `string` | 是 |  | 上传批次 ID，用于后续查询本次文件拆出的切片。 |
| `document_id` | `string | null` | 否 |  | 关联的 documents 文件 ID，文件基础信息统一保存在 documents 表。 |
| `status` | `string` | 是 |  | 批次状态，例如 pending_review / duplicated。 |
| `chunk_count` | `integer` | 是 |  | 本次上传解析出的切片数量。 |
| `point_count` | `integer` | 是 |  | 写入 Qdrant 的向量点数量；预览阶段通常为 0。 |
| `source_file` | `string | null` | 否 |  | 原始文件名，方便前端展示“上传了哪个资料”。 |
| `duplicate_of` | `string | null` | 否 |  | 如果命中 MD5 去重，这里返回已存在的批次 ID。 |
| `quality_report` | `object` | 否 |  | 切片质量报告，供前端确认发布前查看。 |
| `failed_chunks` | `array[string]` | 否 |  | Failed Chunks |

#### GET `/training/knowledge/batches`

- 说明：分页查询训练资料批次
- 详细说明：分页查询已经上传过的训练资料。
- Operation ID：`list_training_batches_training_knowledge_batches_get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `page` | `query` | 否 | `integer` | `1` | Page |
| `page_size` | `query` | 否 | `integer` | `10` | Page Size |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`TrainingKnowledgeBatchListResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `items` | `array[TrainingKnowledgeBatchResponse]` | 是 |  | Items |
| `total` | `integer` | 是 |  | Total |
| `page` | `integer` | 是 |  | Page |
| `page_size` | `integer` | 是 |  | Page Size |

#### GET `/training/knowledge/batches/{batch_id}/preview`

- 说明：预览训练资料原文
- 详细说明：预览训练资料原文件内容。
- Operation ID：`preview_training_batch_training_knowledge_batches__batch_id__preview_get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `batch_id` | `path` | 是 | `string` |  | Batch Id |
| `max_chars` | `query` | 否 | `integer` | `30000` | Max Chars |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`TrainingKnowledgePreviewResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `batch` | `TrainingKnowledgeBatchResponse` | 是 |  | 被预览的训练资料批次。；对象模型：`TrainingKnowledgeBatchResponse` |
| `preview_type` | `string` | 是 |  | text / document_text，表示当前采用的预览方式。 |
| `content` | `string` | 是 |  | 从原文件抽取出的预览文本。 |
| `truncated` | `boolean` | 是 |  | 是否因为内容过长被截断。 |

#### DELETE `/training/knowledge/batches/{batch_id}`

- 说明：删除训练资料批次
- 详细说明：删除训练资料批次。
- Operation ID：`delete_training_batch_training_knowledge_batches__batch_id__delete`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `batch_id` | `path` | 是 | `string` |  | Batch Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`TrainingKnowledgeDeleteResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `status` | `string` | 是 |  | 固定返回 deleted。 |
| `batch_id` | `string` | 是 |  | 被删除的训练资料批次 ID。 |

#### POST `/training/knowledge/batches/{batch_id}/publish`

- 说明：发布训练资料批次
- 详细说明：人工确认发布训练资料。
- Operation ID：`publish_training_batch_training_knowledge_batches__batch_id__publish_post`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `batch_id` | `path` | 是 | `string` |  | Batch Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`TrainingKnowledgePublishResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `batch_id` | `string` | 是 |  | 被发布的批次 ID。 |
| `status` | `string` | 是 |  | 发布后的状态，通常为 published。 |
| `chunk_count` | `integer` | 是 |  | 发布使用的切片数量。 |
| `point_count` | `integer` | 是 |  | 写入 Qdrant 的向量点数量。 |
| `quality_report` | `object` | 否 |  | 发布时参考的质量报告。 |

#### POST `/training/knowledge/batches/{batch_id}/rollback`

- 说明：回滚训练资料版本
- 详细说明：回滚训练资料到指定历史版本。
- Operation ID：`rollback_training_batch_training_knowledge_batches__batch_id__rollback_post`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `batch_id` | `path` | 是 | `string` |  | Batch Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`TrainingKnowledgeRollbackResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `batch_id` | `string` | 是 |  | 回滚后恢复为当前版本的批次 ID。 |
| `status` | `string` | 是 |  | 回滚后的状态，通常为 published。 |
| `version_group_id` | `string` | 是 |  | 版本组 ID。 |
| `version_no` | `integer` | 是 |  | 回滚到的版本号。 |
| `chunk_count` | `integer` | 是 |  | 重新写入的切片数量。 |
| `point_count` | `integer` | 是 |  | 重新写入 Qdrant 的向量点数量。 |
| `quality_report` | `object` | 否 |  | 回滚版本的质量报告。 |

#### POST `/training/knowledge/batches/{batch_id}/reparse`

- 说明：重新切分训练资料批次
- 详细说明：重新切分未发布训练资料。
- Operation ID：`reparse_training_batch_training_knowledge_batches__batch_id__reparse_post`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `batch_id` | `path` | 是 | `string` |  | Batch Id |
| `use_llm_fallback` | `query` | 否 | `boolean` | `True` | Use Llm Fallback |
| `model_mode` | `query` | 否 | `string | null` |  | Model Mode |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`TrainingKnowledgeReparseResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `batch_id` | `string` | 是 |  | 被重新切分的批次 ID。 |
| `status` | `string` | 是 |  | 重新切分后的状态，通常为 pending_review。 |
| `chunk_count` | `integer` | 是 |  | 重新生成的切片数量。 |
| `point_count` | `integer` | 是 |  | 预览阶段固定为 0。 |
| `source_file` | `string | null` | 否 |  | 原始文件名。 |
| `quality_report` | `object` | 否 |  | 重新切分后的质量报告。 |

#### GET `/training/knowledge/batches/{batch_id}/versions`

- 说明：查询训练资料版本链
- 详细说明：查询训练资料版本链。
- Operation ID：`list_training_batch_versions_training_knowledge_batches__batch_id__versions_get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `batch_id` | `path` | 是 | `string` |  | Batch Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`TrainingKnowledgeVersionListResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `version_group_id` | `string` | 是 |  | 版本组 ID。 |
| `items` | `array[TrainingKnowledgeBatchResponse]` | 是 |  | 同版本组内的批次列表，按版本号倒序。 |

#### GET `/training/knowledge/batches/{batch_id}/chunks`

- 说明：查询训练资料切片预览
- 详细说明：查询训练知识上传批次的切片。
- Operation ID：`list_training_chunks_training_knowledge_batches__batch_id__chunks_get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `batch_id` | `path` | 是 | `string` |  | Batch Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`TrainingKnowledgeChunkListResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `batch_id` | `string` | 是 |  | Batch Id |
| `chunks` | `array[TrainingKnowledgeChunkResponse]` | 是 |  | Chunks |

#### POST `/training/plans`

- 说明：创建销售训练方案
- 详细说明：创建训练方案，训练名称允许重复，每条记录用 plan_id 区分。
- Operation ID：`create_training_plan_training_plans_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`application/json`
- Schema：`TrainingPlanCreateRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan_name` | `string` | 是 |  | 训练名称，允许同名，使用 plan_id 区分记录。 |
| `trainee` | `TraineeProfileRequest` | 是 |  | 学员画像快照。；对象模型：`TraineeProfileRequest` |
| `profile_type` | `string` | 否 | `overseas_bd` | 客户画像类型。 |
| `selected_fields` | `object` | 否 |  | 客户画像字段快照。 |
| `scenario_description` | `string` | 是 |  | 训练场景描述。 |
| `extra_details` | `string` | 否 | `` | 补充细节。 |
| `model_mode` | `string | null` | 否 |  | 默认模型档位。 |

响应体：

- Content-Type：`application/json`
- Schema：`TrainingPlanDetailResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan` | `TrainingPlanSummaryResponse` | 是 |  | 对象模型：`TrainingPlanSummaryResponse` |
| `trainee` | `object` | 否 |  | Trainee |
| `selected_fields` | `object` | 否 |  | Selected Fields |
| `scenario_description` | `string` | 否 | `` | Scenario Description |
| `extra_details` | `string` | 否 | `` | Extra Details |
| `visible_profile` | `object` | 否 |  | Visible Profile |
| `hidden_profile` | `object` | 否 |  | Hidden Profile |
| `role_profile` | `object` | 否 |  | Role Profile |
| `role_confirm_card` | `object` | 否 |  | Role Confirm Card |
| `retrieved_cases` | `array[object]` | 否 |  | Retrieved Cases |
| `goal_setting` | `GoalSettingResponse | null` | 否 |  |  |

#### GET `/training/plans`

- 说明：分页查询销售训练方案
- 详细说明：分页查询训练方案列表。
- Operation ID：`list_training_plans_training_plans_get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `page` | `query` | 否 | `integer` | `1` | Page |
| `page_size` | `query` | 否 | `integer` | `10` | Page Size |
| `keyword` | `query` | 否 | `string | null` |  | Keyword |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`TrainingPlanListResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `items` | `array[TrainingPlanSummaryResponse]` | 是 |  | Items |
| `total` | `integer` | 是 |  | Total |
| `page` | `integer` | 是 |  | Page |
| `page_size` | `integer` | 是 |  | Page Size |

#### GET `/training/plans/{plan_id}`

- 说明：查询销售训练方案详情
- 详细说明：查看训练方案每一步详情。
- Operation ID：`get_training_plan_training_plans__plan_id__get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `plan_id` | `path` | 是 | `string` |  | Plan Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`TrainingPlanDetailResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan` | `TrainingPlanSummaryResponse` | 是 |  | 对象模型：`TrainingPlanSummaryResponse` |
| `trainee` | `object` | 否 |  | Trainee |
| `selected_fields` | `object` | 否 |  | Selected Fields |
| `scenario_description` | `string` | 否 | `` | Scenario Description |
| `extra_details` | `string` | 否 | `` | Extra Details |
| `visible_profile` | `object` | 否 |  | Visible Profile |
| `hidden_profile` | `object` | 否 |  | Hidden Profile |
| `role_profile` | `object` | 否 |  | Role Profile |
| `role_confirm_card` | `object` | 否 |  | Role Confirm Card |
| `retrieved_cases` | `array[object]` | 否 |  | Retrieved Cases |
| `goal_setting` | `GoalSettingResponse | null` | 否 |  |  |

#### PUT `/training/plans/{plan_id}`

- 说明：更新销售训练方案
- 详细说明：修改训练方案某一步，并按依赖关系标记后续步骤需要重新生成。
- Operation ID：`update_training_plan_training_plans__plan_id__put`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `plan_id` | `path` | 是 | `string` |  | Plan Id |

请求体：

- Content-Type：`application/json`
- Schema：`TrainingPlanUpdateRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan_name` | `string | null` | 否 |  | 训练名称，允许同名。 |
| `trainee` | `TraineeProfileRequest | null` | 否 |  | 修改学员画像会影响角色和后续阶段。 |
| `profile_type` | `string | null` | 否 |  | 修改客户画像类型会影响角色和后续阶段。 |
| `selected_fields` | `object | null` | 否 |  | 修改客户画像字段会影响角色和后续阶段。 |
| `scenario_description` | `string | null` | 否 |  | 修改场景会影响角色和后续阶段。 |
| `extra_details` | `string | null` | 否 |  | 修改补充细节会影响角色和后续阶段。 |
| `model_mode` | `string | null` | 否 |  | 只修改模型档位不强制重新生成。 |
| `role_confirm_card` | `object | null` | 否 |  | 只人工微调确认卡，不影响阶段。 |
| `visible_profile` | `object | null` | 否 |  | 人工微调可见画像，不影响阶段。 |
| `hidden_profile` | `object | null` | 否 |  | 修改隐藏画像会影响阶段和评分。 |
| `role_profile` | `object | null` | 否 |  | 修改扮演画像会影响阶段和评分。 |
| `training_purpose` | `string | null` | 否 |  | 人工修改训练宗旨，会影响后续训练会话展示。 |
| `round_limit` | `integer | null` | 否 |  | 人工修改训练轮数，限制在 1-100。 |
| `stages` | `array[GoalStage] | null` | 否 |  | 修改训练阶段会影响评分设置。 |
| `scoring_rules` | `object | null` | 否 |  | 只修改评分规则不影响前置步骤。 |

响应体：

- Content-Type：`application/json`
- Schema：`TrainingPlanDetailResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan` | `TrainingPlanSummaryResponse` | 是 |  | 对象模型：`TrainingPlanSummaryResponse` |
| `trainee` | `object` | 否 |  | Trainee |
| `selected_fields` | `object` | 否 |  | Selected Fields |
| `scenario_description` | `string` | 否 | `` | Scenario Description |
| `extra_details` | `string` | 否 | `` | Extra Details |
| `visible_profile` | `object` | 否 |  | Visible Profile |
| `hidden_profile` | `object` | 否 |  | Hidden Profile |
| `role_profile` | `object` | 否 |  | Role Profile |
| `role_confirm_card` | `object` | 否 |  | Role Confirm Card |
| `retrieved_cases` | `array[object]` | 否 |  | Retrieved Cases |
| `goal_setting` | `GoalSettingResponse | null` | 否 |  |  |

#### POST `/training/profiles/generate`

- 说明：生成 AI 客户角色
- 详细说明：生成 AI 陪练角色。
- Operation ID：`generate_role_profile_training_profiles_generate_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`application/json`
- Schema：`RoleGenerateRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan_id` | `string | null` | 否 |  | 可选：关联训练方案；为空时保持旧的临时生成流程。 |
| `trainee` | `TraineeProfileRequest` | 是 |  | 嵌套模型，类似 Java DTO 里包含另一个 DTO。；对象模型：`TraineeProfileRequest` |
| `profile_type` | `string` | 否 | `overseas_bd` | 客户画像类型。 |
| `selected_fields` | `object` | 否 |  | 前端选择的客户画像字段，先用 dict 保持扩展性。 |
| `scenario_description` | `string` | 是 |  | 场景描述必填，是生成角色的重要上下文。 |
| `extra_details` | `string` | 否 | `` | 补充细节，可为空字符串。 |
| `model_mode` | `string | null` | 否 |  | Python 3.10 写法：str | None 等价于 Optional[str]。 |

响应体：

- Content-Type：`application/json`
- Schema：`RoleGenerateResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `profile_id` | `string` | 是 |  | AI 客户画像 ID。 |
| `visible_profile` | `object` | 是 |  | 可展示给管理员/教练看的客户画像摘要。 |
| `hidden_profile` | `object` | 否 |  | 教练配置页可查看；训练对话中不会向学员原文暴露。 |
| `role_profile` | `object` | 是 |  | AI 客户实际扮演时使用的角色设定。 |
| `role_confirm_card` | `object` | 是 |  | 前端确认卡片，适合直接渲染。 |
| `hidden_summary` | `string` | 是 |  | 隐藏画像摘要提示，不暴露具体隐藏心理。 |
| `retrieved_cases` | `array[object]` | 否 |  | 生成角色时召回的案例证据。 |
| `knowledge_facts` | `array[string]` | 否 |  | 提炼给前端展示的关键事实。 |

#### POST `/training/profiles/scenario/polish`

- 说明：润色训练场景描述
- 详细说明：根据客户画像字段润色训练场景描述。
- Operation ID：`polish_training_scenario_training_profiles_scenario_polish_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`application/json`
- Schema：`ScenarioPolishRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `profile_type` | `string` | 否 | `overseas_bd` | 当前选择的客户画像类型。 |
| `selected_fields` | `object` | 否 |  | 当前客户画像字段，用来控制润色方向。 |
| `scenario_description` | `string` | 是 |  | 原始场景描述，按钮点击时从前端文本框传入。 |
| `extra_details` | `string` | 否 | `` | 补充细节，会被模型融合进更自然的场景表达。 |
| `model_mode` | `string | null` | 否 |  | 沿用销售陪练页选择的模型档位。 |

响应体：

- Content-Type：`application/json`
- Schema：`ScenarioPolishResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `polished_scenario` | `string` | 是 |  | 润色后的场景描述，前端会回填到场景描述输入框。 |
| `original_scenario` | `string` | 是 |  | 原始场景描述，方便前端或日志排查润色前后差异。 |

#### POST `/training/profiles/supplement-questions/generate`

- 说明：生成角色补充问答题
- 详细说明：生成 AI 陪练角色前的补充问答题。
- Operation ID：`generate_role_supplement_questions_training_profiles_supplement_questions_generate_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`application/json`
- Schema：`RoleGenerateRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan_id` | `string | null` | 否 |  | 可选：关联训练方案；为空时保持旧的临时生成流程。 |
| `trainee` | `TraineeProfileRequest` | 是 |  | 嵌套模型，类似 Java DTO 里包含另一个 DTO。；对象模型：`TraineeProfileRequest` |
| `profile_type` | `string` | 否 | `overseas_bd` | 客户画像类型。 |
| `selected_fields` | `object` | 否 |  | 前端选择的客户画像字段，先用 dict 保持扩展性。 |
| `scenario_description` | `string` | 是 |  | 场景描述必填，是生成角色的重要上下文。 |
| `extra_details` | `string` | 否 | `` | 补充细节，可为空字符串。 |
| `model_mode` | `string | null` | 否 |  | Python 3.10 写法：str | None 等价于 Optional[str]。 |

响应体：

- Content-Type：`application/json`
- Schema：`SupplementQuestionGenerateResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `questions` | `array[SupplementQuestion]` | 是 |  | Questions |

#### POST `/training/profiles/{profile_id}/goal-settings/generate`

- 说明：生成训练目标和评分设置
- 详细说明：生成一期开放式训练设置。
- Operation ID：`generate_goal_setting_training_profiles__profile_id__goal_settings_generate_post`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `profile_id` | `path` | 是 | `string` |  | Profile Id |

请求体：

- Content-Type：`application/json`
- Schema：`GoalSettingGenerateRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan_id` | `string | null` | 否 |  | 可选：关联训练方案，生成后会更新方案的训练阶段状态。 |
| `trainee_id` | `string` | 是 |  | 学员 ID。 |
| `training_mode` | `string` | 否 | `open` | 一期只支持 open，流程式二期再扩展。 |
| `model_mode` | `string | null` | 否 |  | 使用哪个模型档位生成目标。 |

响应体：

- Content-Type：`application/json`
- Schema：`GoalSettingResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `setting_id` | `string` | 是 |  | 训练设置 ID。 |
| `profile_id` | `string` | 是 |  | 对应的 AI 客户画像 ID。 |
| `training_mode` | `string` | 是 |  | 训练方式，一期为 open。 |
| `training_purpose` | `string` | 是 |  | 训练宗旨，限制较短，适合标题展示。 |
| `round_limit` | `integer` | 是 |  | LLM 动态给出的训练轮数，后端会限制在 5-100。 |
| `stages` | `array[GoalStage]` | 是 |  | 阶段配置，一期只有一个元素。 |
| `scoring_rules` | `object` | 否 |  | 评分设置：固定通用 40 分 + LLM 阶段 60 分。 |
| `status` | `string` | 是 |  | 设置状态，例如 confirmed。 |

#### POST `/training/sessions`

- 说明：开始销售训练会话
- 详细说明：开始训练会话。
- Operation ID：`start_training_session_training_sessions_post`

请求参数：无 path/query 参数。

请求体：

- Content-Type：`application/json`
- Schema：`TrainingSessionStartRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `profile_id` | `string` | 是 |  | 使用哪个 AI 客户画像。 |
| `setting_id` | `string` | 是 |  | 使用哪个训练目标设置。 |
| `trainee_id` | `string` | 是 |  | 学员 ID。 |
| `response_mode` | `string` | 否 | `stream` | stream=流式，blocking=一次性。 |
| `model_mode` | `string | null` | 否 |  | 开场白使用的模型档位。 |

响应体：

- Content-Type：`application/json`
- Schema：`TrainingSessionResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `session_id` | `string` | 是 |  | 训练会话 ID，后续对话和评分都依赖它。 |
| `profile_id` | `string` | 是 |  | AI 客户画像 ID。 |
| `setting_id` | `string` | 是 |  | 训练设置 ID。 |
| `trainee_id` | `string` | 是 |  | 学员 ID。 |
| `training_mode` | `string` | 是 |  | 一期固定 open。 |
| `response_mode` | `string` | 是 |  | 当前会话默认回复模式。 |
| `current_stage_no` | `integer` | 是 |  | 当前阶段，一期固定 1。 |
| `status` | `string` | 是 |  | active / scoring / completed。 |
| `round_limit` | `integer` | 是 |  | 本场训练最大轮数。 |
| `opening_message` | `string | null` | 否 |  | AI 客户开场白，创建会话时返回给前端首屏展示。 |

#### GET `/training/sessions`

- 说明：分页查询销售训练会话
- 详细说明：分页查询训练会话历史。
- Operation ID：`list_training_sessions_training_sessions_get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `page` | `query` | 否 | `integer` | `1` | Page |
| `page_size` | `query` | 否 | `integer` | `10` | Page Size |
| `trainee_id` | `query` | 否 | `string | null` |  | Trainee Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`TrainingSessionListResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `items` | `array[TrainingSessionSummaryResponse]` | 是 |  | Items |
| `total` | `integer` | 是 |  | Total |
| `page` | `integer` | 是 |  | Page |
| `page_size` | `integer` | 是 |  | Page Size |

#### GET `/training/sessions/{session_id}`

- 说明：查询销售训练复盘详情
- 详细说明：查询训练会话复盘详情。
- Operation ID：`get_training_session_detail_training_sessions__session_id__get`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `session_id` | `path` | 是 | `string` |  | Session Id |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`TrainingSessionDetailResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `session` | `TrainingSessionSummaryResponse` | 是 |  | 对象模型：`TrainingSessionSummaryResponse` |
| `turns` | `array[TrainingTurnRecordResponse]` | 是 |  | Turns |
| `visible_profile` | `object` | 否 |  | Visible Profile |
| `hidden_profile` | `object` | 否 |  | Hidden Profile |
| `role_profile` | `object` | 否 |  | Role Profile |
| `role_confirm_card` | `object` | 否 |  | Role Confirm Card |
| `goal_setting` | `object` | 否 |  | Goal Setting |
| `knowledge_facts` | `array[string]` | 否 |  | Knowledge Facts |
| `score` | `TrainingScoreResponse | null` | 否 |  |  |

#### POST `/training/sessions/{session_id}/turns`

- 说明：提交学员回复
- 详细说明：提交学员回复，支持一次性或流式返回。
- Operation ID：`submit_training_turn_training_sessions__session_id__turns_post`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `session_id` | `path` | 是 | `string` |  | Session Id |
| `stream` | `query` | 否 | `boolean` | `False` | Stream |

请求体：

- Content-Type：`application/json`
- Schema：`TrainingTurnRequest`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `message` | `string` | 是 |  | 学员本轮回复。 |
| `response_mode` | `string` | 否 | `stream` | 本轮使用流式还是一次性。 |
| `model_mode` | `string | null` | 否 |  | 本轮 AI 客户回复使用的模型档位。 |

响应体：SSE 文本流，事件详见下方“流式事件”。

流式事件：

| 事件名 | 说明 |
| --- | --- |
| `retrieval_done` | 本轮训练检索完成，包含 retrieved_chunk_ids 和 evidence。 |
| `customer_delta` | AI 客户回复增量文本，data.content 是本次新增内容。 |
| `stage_decision` | 阶段和会话状态，包含 stage_status、session_status。 |
| `turn_done` | 本轮训练完成，data 是 TrainingTurnResponse 结构。 |
| `error` | 训练流式处理失败，data.error 是错误信息。 |

#### POST `/training/sessions/{session_id}/final-score`

- 说明：生成最终训练评分
- 详细说明：结束训练并生成评分报告。
- Operation ID：`final_score_training_sessions__session_id__final_score_post`

请求参数：

| 参数 | 位置 | 必填 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `session_id` | `path` | 是 | `string` |  | Session Id |
| `model_mode` | `query` | 否 | `string | null` |  | Model Mode |

请求体：无。

响应体：

- Content-Type：`application/json`
- Schema：`TrainingScoreResponse`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `score_id` | `string` | 是 |  | Score Id |
| `session_id` | `string` | 是 |  | Session Id |
| `total_score` | `integer` | 是 |  | Total Score |
| `level` | `string` | 是 |  | Level |
| `is_passed` | `boolean` | 是 |  | Is Passed |
| `general_score` | `integer` | 是 |  | General Score |
| `stage_score` | `integer` | 是 |  | Stage Score |
| `penalty_score` | `integer` | 是 |  | Penalty Score |
| `report` | `object` | 是 |  | Report |

## 数据模型附录

### `Body_preview_knowledge_file_knowledge_upload_preview_post`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `file` | `string` | 是 |  | File |

### `Body_upload_training_knowledge_training_knowledge_upload_post`

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `file` | `string` | 是 |  | File |
| `source_type` | `string` | 否 | `lms_case` | Source Type |
| `model_mode` | `string | null` | 否 |  | Model Mode |
| `created_by` | `string | null` | 否 |  | Created By |

### `ChatRequest`

前端聊天请求体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `message` | `string` | 是 |  | 用户输入的问题；不能为空字符串 |
| `user_id` | `string | null` | 否 |  | 当前会话用户 ID；可为空，工具层会兜底随机用户 |
| `conversation_id` | `string | null` | 否 |  | 当前会话 ID；为空时后端创建新会话 |
| `model_mode` | `string | null` | 否 |  | 回答模型档位，具体可选值和默认值来自 model_mode 字典 |
| `collection_name` | `string | null` | 否 |  | 本次聊天要检索的 Qdrant collection；为空时使用默认 collection |

### `ChatResponse`

一次性接口 `/chat` 的响应体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `answer` | `string` | 是 |  | 一次性接口返回的完整最终回答 |
| `conversation_id` | `string` | 是 |  | 当前会话 ID；前端后续请求需要继续携带 |
| `first_token_ms` | `number | null` | 否 |  | 首字/首片耗时；一次性接口没有真实首字，默认等于 total_ms |
| `total_ms` | `number | null` | 否 |  | 本次请求总耗时 |

### `ConversationDeleteResponse`

删除聊天会话响应体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `status` | `string` | 是 |  | 固定返回 deleted |
| `conversation_id` | `string` | 是 |  | 被删除的会话 ID |

### `ConversationDetailResponse`

会话详情响应体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `conversation` | `ConversationSummaryResponse` | 是 |  | 会话摘要；对象模型：`ConversationSummaryResponse` |
| `messages` | `array[ConversationMessageResponse]` | 是 |  | 会话全部消息 |

### `ConversationListResponse`

会话分页列表响应体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `items` | `array[ConversationSummaryResponse]` | 是 |  | 当前页会话摘要 |
| `total` | `integer` | 是 |  | 总会话数 |
| `page` | `integer` | 是 |  | 当前页码 |
| `page_size` | `integer` | 是 |  | 每页数量 |

### `ConversationMessageResponse`

会话详情中的单条消息。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `message_id` | `string` | 是 |  | 消息唯一 ID |
| `conversation_id` | `string` | 是 |  | 所属会话 ID |
| `sequence_no` | `integer` | 是 |  | 会话内顺序号 |
| `role` | `string` | 是 |  | user/assistant/system |
| `content` | `string` | 是 |  | 消息正文 |
| `content_type` | `string` | 是 |  | 消息类型，默认 text |
| `model_name` | `string | null` | 否 |  | 助手消息使用的模型名 |
| `token_count` | `integer | null` | 否 |  | token 数量，当前可为空 |
| `first_token_ms` | `number | null` | 否 |  | 助手首字/首片耗时；旧数据没有该字段时为空 |
| `total_ms` | `number | null` | 否 |  | 助手完整回答耗时；旧数据没有该字段时为空 |
| `created_at` | `string` | 是 |  | 创建时间 |

### `ConversationSummaryResponse`

会话列表中的单个会话摘要。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `conversation_id` | `string` | 是 |  | 会话唯一 ID |
| `user_id` | `string | null` | 否 |  | 会话所属用户 ID |
| `title` | `string | null` | 否 |  | 会话标题，通常来自首条用户问题 |
| `status` | `string` | 是 |  | 会话状态 |
| `message_count` | `integer` | 是 |  | 会话消息数量 |
| `created_at` | `string` | 是 |  | 创建时间 |
| `updated_at` | `string` | 是 |  | 更新时间 |
| `last_message_at` | `string | null` | 否 |  | 最后一条消息时间 |

### `DebugRetrieveRequest`

RAG 检索调试请求体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `query` | `string` | 是 |  | 要调试的用户问题 |

### `DictionaryGroupResponse`

单个字典分组响应体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `dictionary_code` | `string` | 是 |  | 字典编码 |
| `dictionary_name` | `string` | 是 |  | 字典名称 |
| `items` | `array[DictionaryItemResponse]` | 否 |  | 当前字典的树形字典项 |

### `DictionaryItemResponse`

字典项响应体，支持通过 children 表示多层级字典。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `dictionary_item_id` | `string` | 是 |  | 字典项唯一 ID |
| `dictionary_code` | `string` | 是 |  | 字典编码，例如 document_structure |
| `dictionary_name` | `string` | 是 |  | 字典名称，例如 文档结构类型 |
| `item_code` | `string` | 是 |  | 字典项编码，例如 qa |
| `item_name` | `string` | 是 |  | 字典项名称，例如 问答型 |
| `parent_item_id` | `string | null` | 否 |  | 父级字典项 ID；为空表示一级项 |
| `item_level` | `integer` | 是 |  | 层级，从 1 开始 |
| `sort_order` | `integer` | 是 |  | 同级排序号 |
| `enabled` | `boolean` | 是 |  | 是否启用 |
| `description` | `string | null` | 否 |  | 字典项说明 |
| `metadata` | `object` | 否 |  | 前端展示或业务扩展元数据 |
| `children` | `array[DictionaryItemResponse]` | 否 |  | 子级字典项 |

### `DictionaryItemUpsertRequest`

字典项新增或更新请求体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `dictionary_code` | `string` | 是 |  | 字典编码，例如 model_mode |
| `dictionary_name` | `string` | 是 |  | 字典名称，例如 回答模型档位 |
| `item_code` | `string` | 是 |  | 字典项编码，同一字典下唯一 |
| `item_name` | `string` | 是 |  | 字典项展示名称 |
| `parent_item_id` | `string | null` | 否 |  | 父级字典项 ID；为空表示一级项 |
| `parent_item_code` | `string | null` | 否 |  | 父级字典项编码；创建子项时可用编码定位父级 |
| `sort_order` | `integer` | 否 | `0` | 同级排序号 |
| `enabled` | `boolean` | 否 | `True` | 是否启用 |
| `description` | `string | null` | 否 |  | 字典项说明 |
| `metadata` | `object` | 否 |  | 业务扩展元数据，例如 collection_name/keyword |

### `ExamAnswerAnalysis`

单题作答分析。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `is_correct` | `boolean` | 是 |  | 是否判定正确 |
| `score` | `number` | 是 |  | 本题得分 |
| `max_score` | `number` | 是 |  | 本题满分 |
| `correct_answer` | `Correct Answer` | 否 |  | 正确答案 |
| `reference_answer` | `string` | 是 |  | 原始参考答案 |
| `hit_points` | `array[string]` | 否 |  | 命中要点 |
| `missing_points` | `array[string]` | 否 |  | 遗漏点 |
| `wrong_points` | `array[string]` | 否 |  | 错误点 |
| `comment` | `string` | 否 | `` | 点评 |

### `ExamAnswerRequest`

提交单轮考试答案请求体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `answer` | `string | array[string]` | 否 | `` | 用户答案；多选题可传字符串数组 |

### `ExamAnswerResponse`

提交答案响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `session` | `ExamSessionSummary` | 是 |  | 更新后的考试会话；对象模型：`ExamSessionSummary` |
| `answered_question` | `ExamConversationQuestion` | 是 |  | 已作答题目；对象模型：`ExamConversationQuestion` |
| `analysis` | `ExamAnswerAnalysis` | 是 |  | 本题分析；对象模型：`ExamAnswerAnalysis` |
| `next_question` | `ExamConversationQuestion | null` | 否 |  | 下一轮题目；考试完成时为空 |

### `ExamConversationQuestion`

前端对话式考试展示题目。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `exam_question_id` | `string` | 是 |  | 本场考试内的题目编号 |
| `round_no` | `integer` | 是 |  | 当前轮次，从 1 开始 |
| `question_type` | `enum` | 是 |  | 题型；可选值：`single_choice`、`multiple_choice`、`true_false`、`short_answer`、`fill_blank` |
| `prompt` | `string` | 是 |  | 题目文本 |
| `options` | `array[string]` | 否 |  | 选项；非选择题为空 |
| `max_score` | `number` | 是 |  | 本题满分，所有轮次加起来总分为 100 |
| `source` | `ExamQuestionSource` | 是 |  | 来源信息；对象模型：`ExamQuestionSource` |

### `ExamHistoryListResponse`

考试历史分页响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `items` | `array[ExamSessionSummary]` | 是 |  | 当前页考试记录 |
| `total` | `integer` | 是 |  | 总数 |
| `page` | `integer` | 是 |  | 当前页 |
| `page_size` | `integer` | 是 |  | 每页数量 |

### `ExamQuestionRecord`

考试详情中的单题记录。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `question` | `ExamConversationQuestion` | 是 |  | 题目信息；对象模型：`ExamConversationQuestion` |
| `user_answer` | `string | null` | 否 |  | 用户答案 |
| `analysis` | `ExamAnswerAnalysis | null` | 否 |  | 分析结果；未作答时为空 |
| `answered_at` | `string | null` | 否 |  | 作答时间 |

### `ExamQuestionSource`

考试题目来源信息。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `document_id` | `string | null` | 否 |  | 来源文件编号 |
| `filename` | `string | null` | 否 |  | 来源文件名 |
| `section_path` | `string | null` | 否 |  | 来源目录路径 |
| `source_page` | `integer | null` | 否 |  | 来源页码 |

### `ExamSectionResponse`

考试题源目录响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `section_path` | `string` | 是 |  | 目录路径 |
| `question_count` | `integer` | 是 |  | 该目录下可抽取的题目数量 |

### `ExamSectionsResponse`

考试题源目录列表响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `collection_name` | `string` | 是 |  | 向量库名称 |
| `document_id` | `string | null` | 否 |  | 文件编号 |
| `sections` | `array[ExamSectionResponse]` | 否 |  | 目录列表 |

### `ExamSessionDetailResponse`

考试会话详情响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `session` | `ExamSessionSummary` | 是 |  | 考试会话摘要；对象模型：`ExamSessionSummary` |
| `questions` | `array[ExamQuestionRecord]` | 是 |  | 全部题目和用户答案 |

### `ExamSessionSummary`

考试会话摘要。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `session_id` | `string` | 是 |  | 考试会话编号 |
| `user_id` | `string | null` | 否 |  | 用户编号 |
| `title` | `string` | 是 |  | 会话标题 |
| `collection_name` | `string` | 是 |  | 向量库名称 |
| `document_id` | `string | null` | 否 |  | 文件编号 |
| `filename` | `string | null` | 否 |  | 文件名 |
| `section_path` | `string | null` | 否 |  | 目录路径 |
| `round_count` | `integer` | 是 |  | 总轮数 |
| `answered_count` | `integer` | 是 |  | 已答题数 |
| `total_score` | `number` | 是 |  | 当前总分 |
| `max_score` | `number` | 是 |  | 满分 |
| `status` | `string` | 是 |  | active/completed |
| `current_round` | `integer` | 是 |  | 当前轮次 |
| `created_at` | `string` | 是 |  | 创建时间 |
| `updated_at` | `string` | 是 |  | 更新时间 |
| `completed_at` | `string | null` | 否 |  | 完成时间 |

### `ExamStartRequest`

开始对话式考试请求体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `title` | `string` | 是 |  | 本场测评名称；历史记录中展示 |
| `collection_name` | `string | null` | 否 |  | 题源所在 Qdrant collection；为空时使用默认 collection |
| `document_id` | `string | null` | 否 |  | 指定题源文件；为空时从整个 collection 抽题 |
| `section_path` | `string | null` | 否 |  | 指定目录路径；为空时不按目录过滤 |
| `user_id` | `string | null` | 否 |  | 考试用户编号 |
| `round_count` | `integer` | 否 | `5` | 考试轮数 |
| `question_types` | `array[enum]` | 否 |  | 允许随机生成的题型 |
| `model_mode` | `string | null` | 否 |  | 主观题分析模型档位 |
| `seed` | `integer | null` | 否 |  | 随机种子；传入后同范围抽题更容易复现 |

### `ExamStartResponse`

开始对话式考试响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `session` | `ExamSessionSummary` | 是 |  | 考试会话摘要；对象模型：`ExamSessionSummary` |
| `current_question` | `ExamConversationQuestion` | 是 |  | 第一轮题目；对象模型：`ExamConversationQuestion` |

### `GoalSettingGenerateRequest`

开放式训练设置生成请求。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan_id` | `string | null` | 否 |  | 可选：关联训练方案，生成后会更新方案的训练阶段状态。 |
| `trainee_id` | `string` | 是 |  | 学员 ID。 |
| `training_mode` | `string` | 否 | `open` | 一期只支持 open，流程式二期再扩展。 |
| `model_mode` | `string | null` | 否 |  | 使用哪个模型档位生成目标。 |

### `GoalSettingResponse`

开放式训练设置响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `setting_id` | `string` | 是 |  | 训练设置 ID。 |
| `profile_id` | `string` | 是 |  | 对应的 AI 客户画像 ID。 |
| `training_mode` | `string` | 是 |  | 训练方式，一期为 open。 |
| `training_purpose` | `string` | 是 |  | 训练宗旨，限制较短，适合标题展示。 |
| `round_limit` | `integer` | 是 |  | LLM 动态给出的训练轮数，后端会限制在 5-100。 |
| `stages` | `array[GoalStage]` | 是 |  | 阶段配置，一期只有一个元素。 |
| `scoring_rules` | `object` | 否 |  | 评分设置：固定通用 40 分 + LLM 阶段 60 分。 |
| `status` | `string` | 是 |  | 设置状态，例如 confirmed。 |

### `GoalStage`

训练阶段。一期开放式固定只有一个阶段。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `stage_no` | `integer` | 否 | `1` | 阶段序号，一期固定 1。 |
| `stage_name` | `string` | 是 |  | 阶段名称。 |
| `core_goal` | `string` | 是 |  | 本次训练核心目标。 |
| `success_conditions` | `array[string]` | 是 |  | 达成条件。 |
| `failure_conditions` | `array[string]` | 是 |  | 失败条件。 |

### `HealthResponse`

健康检查响应体，用于前端侧边栏展示服务状态。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `status` | `string` | 是 |  | 整体状态；ok 表示全部可用，degraded 表示部分依赖不可用 |
| `qdrant` | `string` | 是 |  | Qdrant 状态；ok 或 unavailable |
| `redis` | `string` | 否 | `unavailable` | Redis 状态；ok 或 unavailable |
| `collection_name` | `string` | 是 |  | 当前项目使用的 Qdrant collection 名称 |
| `collections` | `array[string]` | 否 |  | Qdrant 中已有的 collection 列表 |
| `collection_points` | `object` | 否 |  | 每个 collection 在 Qdrant 中的向量点数量 |

### `KnowledgeBulkReindexResponse`

全部重建索引响应体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `status` | `string` | 是 |  | ok 或 partial_failed |
| `total` | `integer` | 是 |  | 参与重建的文件数 |
| `succeeded` | `integer` | 是 |  | 成功数量 |
| `failed` | `integer` | 是 |  | 失败数量 |
| `results` | `array[KnowledgeReindexResult]` | 是 |  | 每个文件的结果 |

### `KnowledgeDeleteResponse`

删除知识库文件的响应体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `status` | `string` | 是 |  | 固定返回 deleted |
| `document_id` | `string` | 是 |  | 被删除的文件 ID |

### `KnowledgeFilePreviewResponse`

已入库知识库文件的预览响应体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `document` | `KnowledgeFileResponse` | 是 |  | 被预览的文件元数据；对象模型：`KnowledgeFileResponse` |
| `preview_type` | `string` | 是 |  | text/pdf_text/unsupported，表示本次预览内容的来源类型 |
| `content` | `string` | 是 |  | 预览文本内容；大文件会按 max_chars 截断 |
| `truncated` | `boolean` | 是 |  | 是否因为超过 max_chars 被截断 |
| `page_count` | `integer | null` | 否 |  | PDF 页数；TXT 文件为空 |

### `KnowledgeFileResponse`

知识库文件响应体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `document_id` | `string` | 是 |  | 文件唯一 ID；删除和重建索引都靠它定位文件 |
| `filename` | `string` | 是 |  | 用户上传时的原始文件名 |
| `file_path` | `string` | 是 |  | MinIO 存储 URI，格式为 minio://桶名/对象路径 |
| `file_type` | `string` | 是 |  | 文件类型，例如 txt/pdf |
| `file_md5` | `string` | 是 |  | 文件内容 MD5；用于判断重复上传 |
| `file_size` | `integer` | 是 |  | 文件大小，单位字节 |
| `status` | `string` | 是 |  | uploaded/indexing/indexed/failed/deleted |
| `version` | `integer` | 是 |  | 文件索引版本；每次 reindex 会递增 |
| `chunk_count` | `integer` | 是 |  | 当前版本写入 Qdrant 的知识单元数量 |
| `collection_name` | `string` | 否 | `agent` | 文件写入的 Qdrant collection |
| `document_type` | `string` | 否 | `text` | 文档结构类型：qa/numbered/text |
| `split_strategy` | `string` | 否 | `recursive` | 文件入库时使用的切分策略：numbered_qa/outline_qa/numbered_segments/recursive |
| `created_at` | `string` | 是 |  | 文件记录创建时间 |
| `updated_at` | `string` | 是 |  | 文件记录最后更新时间 |
| `error_message` | `string | null` | 否 |  | 入库失败时保存错误原因 |

### `KnowledgeReindexResult`

单个文件重建索引结果。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `document_id` | `string` | 是 |  | 文件 ID |
| `filename` | `string` | 是 |  | 文件名 |
| `status` | `string` | 是 |  | indexed 或 failed |
| `message` | `string | null` | 否 |  | 失败原因或成功说明 |

### `KnowledgeUploadConfirmRequest`

上传确认请求体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `upload_id` | `string` | 是 |  | 预览阶段返回的 upload_id |
| `document_type` | `string` | 是 |  | 用户确认后的文档结构类型：qa/numbered/text |
| `split_strategy` | `string` | 是 |  | 用户确认后的切分策略：numbered_qa/outline_qa/numbered_segments/recursive |
| `collection_name` | `string | null` | 否 |  | 用户选择或输入的 Qdrant collection；为空时使用默认 collection |

### `KnowledgeUploadPreviewResponse`

上传预览响应体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `upload_id` | `string` | 是 |  | 临时上传 ID，确认入库时使用 |
| `filename` | `string` | 是 |  | 原始文件名 |
| `file_type` | `string` | 是 |  | 文件类型 |
| `file_size` | `integer` | 是 |  | 文件大小 |
| `file_md5` | `string` | 是 |  | 文件 MD5 |
| `duplicate` | `boolean` | 否 | `False` | 是否与已有 active 文件重复 |
| `duplicate_document` | `KnowledgeFileResponse | null` | 否 |  | 重复时对应的已有文件 |
| `detected_type` | `string` | 是 |  | 系统识别的文档类型 |
| `split_strategy` | `string` | 是 |  | 系统建议的切分策略 |
| `confidence` | `number` | 是 |  | 识别置信度 |
| `reasons` | `array[string]` | 否 |  | 识别原因 |
| `llm_used` | `boolean` | 否 | `False` | 是否使用了 LLM 兜底 |
| `sample_text` | `string` | 否 | `` | 抽样文本，给前端预览 |

### `KnowledgeUploadRecommendRequest`

上传文件模型推荐请求体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `upload_id` | `string` | 是 |  | 预览阶段返回的 upload_id |

### `KnowledgeUploadRecommendResponse`

上传文件模型推荐响应体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `document_type` | `string` | 是 |  | 模型推荐的文档类型 |
| `split_strategy` | `string` | 是 |  | 模型推荐的切分策略：numbered_qa/outline_qa/numbered_segments/recursive |
| `confidence` | `number` | 是 |  | 模型推荐置信度，范围 0 到 1 |
| `reasons` | `array[string]` | 否 |  | 模型推荐原因 |
| `sample_chars` | `integer` | 是 |  | 实际发送给模型的样本文本字符数 |
| `model_name` | `string` | 是 |  | 本次用于推荐的模型名称 |

### `KnowledgeUploadResponse`

上传知识库文件的响应体。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `status` | `string` | 是 |  | indexed 或 duplicate |
| `message` | `string` | 是 |  | 面向调用方的简短说明 |
| `document` | `KnowledgeFileResponse` | 是 |  | 成功入库或已存在的文件记录；对象模型：`KnowledgeFileResponse` |

### `RoleGenerateRequest`

生成 AI 陪练角色请求。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan_id` | `string | null` | 否 |  | 可选：关联训练方案；为空时保持旧的临时生成流程。 |
| `trainee` | `TraineeProfileRequest` | 是 |  | 嵌套模型，类似 Java DTO 里包含另一个 DTO。；对象模型：`TraineeProfileRequest` |
| `profile_type` | `string` | 否 | `overseas_bd` | 客户画像类型。 |
| `selected_fields` | `object` | 否 |  | 前端选择的客户画像字段，先用 dict 保持扩展性。 |
| `scenario_description` | `string` | 是 |  | 场景描述必填，是生成角色的重要上下文。 |
| `extra_details` | `string` | 否 | `` | 补充细节，可为空字符串。 |
| `model_mode` | `string | null` | 否 |  | Python 3.10 写法：str | None 等价于 Optional[str]。 |

### `RoleGenerateResponse`

生成 AI 陪练角色响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `profile_id` | `string` | 是 |  | AI 客户画像 ID。 |
| `visible_profile` | `object` | 是 |  | 可展示给管理员/教练看的客户画像摘要。 |
| `hidden_profile` | `object` | 否 |  | 教练配置页可查看；训练对话中不会向学员原文暴露。 |
| `role_profile` | `object` | 是 |  | AI 客户实际扮演时使用的角色设定。 |
| `role_confirm_card` | `object` | 是 |  | 前端确认卡片，适合直接渲染。 |
| `hidden_summary` | `string` | 是 |  | 隐藏画像摘要提示，不暴露具体隐藏心理。 |
| `retrieved_cases` | `array[object]` | 否 |  | 生成角色时召回的案例证据。 |
| `knowledge_facts` | `array[string]` | 否 |  | 提炼给前端展示的关键事实。 |

### `ScenarioPolishRequest`

AI 润色训练场景描述请求。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `profile_type` | `string` | 否 | `overseas_bd` | 当前选择的客户画像类型。 |
| `selected_fields` | `object` | 否 |  | 当前客户画像字段，用来控制润色方向。 |
| `scenario_description` | `string` | 是 |  | 原始场景描述，按钮点击时从前端文本框传入。 |
| `extra_details` | `string` | 否 | `` | 补充细节，会被模型融合进更自然的场景表达。 |
| `model_mode` | `string | null` | 否 |  | 沿用销售陪练页选择的模型档位。 |

### `ScenarioPolishResponse`

AI 润色训练场景描述响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `polished_scenario` | `string` | 是 |  | 润色后的场景描述，前端会回填到场景描述输入框。 |
| `original_scenario` | `string` | 是 |  | 原始场景描述，方便前端或日志排查润色前后差异。 |

### `SupplementQuestion`

生成角色前的补充问答题。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `question_id` | `string` | 是 |  | 题目编号，前端回传时用于定位。 |
| `question_no` | `integer` | 是 |  | 展示序号，从 1 开始。 |
| `question` | `string` | 是 |  | 题干。 |
| `options` | `array[SupplementQuestionOption]` | 是 |  | 标准选项，通常 4 个。 |
| `allow_other` | `boolean` | 否 | `True` | 是否允许填写“其他”。 |
| `dimension` | `string` | 否 | `` | 题目关注维度，例如价格、痛点、性格、风险。 |

### `SupplementQuestionGenerateResponse`

补充问答题生成响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `questions` | `array[SupplementQuestion]` | 是 |  | Questions |

### `SupplementQuestionOption`

补充问答选项。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `option_code` | `string` | 是 |  | 选项编码，前端展示为 A/B/C/D。 |
| `option_text` | `string` | 是 |  | 选项文案。 |

### `TraineeProfileRequest`

一期学员画像输入。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `trainee_id` | `string` | 是 |  | Trainee Id |
| `trainee_name` | `string` | 否 | `销售学员` | 学员展示名。 |
| `position_role` | `string` | 否 | `overseas_bd` | 岗位/角色标签。 |
| `experience_level` | `string` | 否 | `junior` | 经验等级，影响 AI 客户追问强度。 |
| `task_goal` | `string` | 否 | `goal_junior` | 任务目标档位：goal_junior / goal_intermediate / goal_senior。 |
| `weakness_tags` | `array[string]` | 否 |  | 学员短板标签，例如价格异议、需求挖掘。 |
| `student_portrait_other` | `string` | 否 | `` | 学员画像补充说明，对应 student_portrait_other 输入型字典项。 |

### `TrainingKnowledgeBatchListResponse`

训练资料批次分页响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `items` | `array[TrainingKnowledgeBatchResponse]` | 是 |  | Items |
| `total` | `integer` | 是 |  | Total |
| `page` | `integer` | 是 |  | Page |
| `page_size` | `integer` | 是 |  | Page Size |

### `TrainingKnowledgeBatchResponse`

训练资料上传批次摘要。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `batch_id` | `string` | 是 |  | 批次 ID，用于点击后查询切片。 |
| `document_id` | `string | null` | 否 |  | 关联的 documents 文件 ID。 |
| `source_type` | `string` | 是 |  | 来源类型，例如 lms_case。 |
| `source_file` | `string` | 是 |  | 上传文件名。 |
| `file_path` | `string | null` | 否 |  | MinIO 存储 URI；原文件预览和排查通过 object_name 临时下载。 |
| `file_md5` | `string | null` | 否 |  | 文件 MD5，用于判断重复上传。 |
| `version_group_id` | `string | null` | 否 |  | 版本组 ID，同一个文件多次发布会归到同一版本组。 |
| `version_no` | `integer` | 否 | `1` | 版本号，从 1 开始递增。 |
| `previous_batch_id` | `string | null` | 否 |  | 上一个版本的批次 ID。 |
| `is_current` | `boolean` | 否 | `False` | 是否为当前参与训练检索的版本。 |
| `profile_type` | `string | null` | 否 |  | 适用客户画像类型。 |
| `task_type` | `string | null` | 否 |  | 训练任务类型。 |
| `industry` | `string | null` | 否 |  | 行业标签。 |
| `difficulty` | `string | null` | 否 |  | 难度标签。 |
| `visibility_default` | `string | null` | 否 |  | 默认可见范围。 |
| `status` | `string` | 是 |  | published / parsing_failed 等。 |
| `chunk_count` | `integer` | 是 |  | 切片数量。 |
| `point_count` | `integer` | 是 |  | 向量点数量。 |
| `error_message` | `string | null` | 否 |  | 入库失败原因。 |
| `quality_report` | `object` | 否 |  | 最近一次切片质量报告。 |
| `created_by` | `string | null` | 否 |  | 上传人。 |
| `created_at` | `string` | 是 |  | 创建时间。 |
| `updated_at` | `string` | 是 |  | 更新时间。 |

### `TrainingKnowledgeChunkListResponse`

某次上传批次的切片列表。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `batch_id` | `string` | 是 |  | Batch Id |
| `chunks` | `array[TrainingKnowledgeChunkResponse]` | 是 |  | Chunks |

### `TrainingKnowledgeChunkResponse`

训练知识切片响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `chunk_id` | `string` | 是 |  | 切片唯一 ID。 |
| `batch_id` | `string` | 是 |  | 所属上传批次。 |
| `case_part` | `string` | 是 |  | 切片业务类型，例如 case_profile / hidden_psychology / scoring_rubric。 |
| `visibility` | `string` | 是 |  | 可见范围：visible / hidden / scoring_only。 |
| `chunk_text` | `string` | 是 |  | 切片正文，会写入向量库用于检索。 |
| `metadata` | `object` | 否 |  | 额外元数据，例如案例标题、案例序号。 |

### `TrainingKnowledgeDeleteResponse`

训练资料删除响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `status` | `string` | 是 |  | 固定返回 deleted。 |
| `batch_id` | `string` | 是 |  | 被删除的训练资料批次 ID。 |

### `TrainingKnowledgePreviewResponse`

训练资料原文件预览响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `batch` | `TrainingKnowledgeBatchResponse` | 是 |  | 被预览的训练资料批次。；对象模型：`TrainingKnowledgeBatchResponse` |
| `preview_type` | `string` | 是 |  | text / document_text，表示当前采用的预览方式。 |
| `content` | `string` | 是 |  | 从原文件抽取出的预览文本。 |
| `truncated` | `boolean` | 是 |  | 是否因为内容过长被截断。 |

### `TrainingKnowledgePublishResponse`

训练资料确认发布响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `batch_id` | `string` | 是 |  | 被发布的批次 ID。 |
| `status` | `string` | 是 |  | 发布后的状态，通常为 published。 |
| `chunk_count` | `integer` | 是 |  | 发布使用的切片数量。 |
| `point_count` | `integer` | 是 |  | 写入 Qdrant 的向量点数量。 |
| `quality_report` | `object` | 否 |  | 发布时参考的质量报告。 |

### `TrainingKnowledgeReparseResponse`

训练资料重新切分响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `batch_id` | `string` | 是 |  | 被重新切分的批次 ID。 |
| `status` | `string` | 是 |  | 重新切分后的状态，通常为 pending_review。 |
| `chunk_count` | `integer` | 是 |  | 重新生成的切片数量。 |
| `point_count` | `integer` | 是 |  | 预览阶段固定为 0。 |
| `source_file` | `string | null` | 否 |  | 原始文件名。 |
| `quality_report` | `object` | 否 |  | 重新切分后的质量报告。 |

### `TrainingKnowledgeRollbackResponse`

训练资料版本回滚响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `batch_id` | `string` | 是 |  | 回滚后恢复为当前版本的批次 ID。 |
| `status` | `string` | 是 |  | 回滚后的状态，通常为 published。 |
| `version_group_id` | `string` | 是 |  | 版本组 ID。 |
| `version_no` | `integer` | 是 |  | 回滚到的版本号。 |
| `chunk_count` | `integer` | 是 |  | 重新写入的切片数量。 |
| `point_count` | `integer` | 是 |  | 重新写入 Qdrant 的向量点数量。 |
| `quality_report` | `object` | 否 |  | 回滚版本的质量报告。 |

### `TrainingKnowledgeUploadResponse`

训练知识上传预览响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `batch_id` | `string` | 是 |  | 上传批次 ID，用于后续查询本次文件拆出的切片。 |
| `document_id` | `string | null` | 否 |  | 关联的 documents 文件 ID，文件基础信息统一保存在 documents 表。 |
| `status` | `string` | 是 |  | 批次状态，例如 pending_review / duplicated。 |
| `chunk_count` | `integer` | 是 |  | 本次上传解析出的切片数量。 |
| `point_count` | `integer` | 是 |  | 写入 Qdrant 的向量点数量；预览阶段通常为 0。 |
| `source_file` | `string | null` | 否 |  | 原始文件名，方便前端展示“上传了哪个资料”。 |
| `duplicate_of` | `string | null` | 否 |  | 如果命中 MD5 去重，这里返回已存在的批次 ID。 |
| `quality_report` | `object` | 否 |  | 切片质量报告，供前端确认发布前查看。 |
| `failed_chunks` | `array[string]` | 否 |  | Failed Chunks |

### `TrainingKnowledgeVersionListResponse`

训练资料版本链响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `version_group_id` | `string` | 是 |  | 版本组 ID。 |
| `items` | `array[TrainingKnowledgeBatchResponse]` | 是 |  | 同版本组内的批次列表，按版本号倒序。 |

### `TrainingPlanCreateRequest`

创建训练方案请求。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan_name` | `string` | 是 |  | 训练名称，允许同名，使用 plan_id 区分记录。 |
| `trainee` | `TraineeProfileRequest` | 是 |  | 学员画像快照。；对象模型：`TraineeProfileRequest` |
| `profile_type` | `string` | 否 | `overseas_bd` | 客户画像类型。 |
| `selected_fields` | `object` | 否 |  | 客户画像字段快照。 |
| `scenario_description` | `string` | 是 |  | 训练场景描述。 |
| `extra_details` | `string` | 否 | `` | 补充细节。 |
| `model_mode` | `string | null` | 否 |  | 默认模型档位。 |

### `TrainingPlanDetailResponse`

训练方案详情响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan` | `TrainingPlanSummaryResponse` | 是 |  | 对象模型：`TrainingPlanSummaryResponse` |
| `trainee` | `object` | 否 |  | Trainee |
| `selected_fields` | `object` | 否 |  | Selected Fields |
| `scenario_description` | `string` | 否 | `` | Scenario Description |
| `extra_details` | `string` | 否 | `` | Extra Details |
| `visible_profile` | `object` | 否 |  | Visible Profile |
| `hidden_profile` | `object` | 否 |  | Hidden Profile |
| `role_profile` | `object` | 否 |  | Role Profile |
| `role_confirm_card` | `object` | 否 |  | Role Confirm Card |
| `retrieved_cases` | `array[object]` | 否 |  | Retrieved Cases |
| `goal_setting` | `GoalSettingResponse | null` | 否 |  |  |

### `TrainingPlanListResponse`

训练方案分页响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `items` | `array[TrainingPlanSummaryResponse]` | 是 |  | Items |
| `total` | `integer` | 是 |  | Total |
| `page` | `integer` | 是 |  | Page |
| `page_size` | `integer` | 是 |  | Page Size |

### `TrainingPlanSummaryResponse`

训练方案列表摘要。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan_id` | `string` | 是 |  | Plan Id |
| `plan_name` | `string` | 是 |  | Plan Name |
| `trainee_id` | `string` | 是 |  | Trainee Id |
| `trainee_name` | `string` | 是 |  | Trainee Name |
| `profile_type` | `string` | 是 |  | Profile Type |
| `model_mode` | `string | null` | 否 |  | Model Mode |
| `role_status` | `string` | 是 |  | Role Status |
| `goal_status` | `string` | 是 |  | Goal Status |
| `score_status` | `string` | 是 |  | Score Status |
| `active_profile_id` | `string | null` | 否 |  | Active Profile Id |
| `active_setting_id` | `string | null` | 否 |  | Active Setting Id |
| `created_at` | `string` | 是 |  | Created At |
| `updated_at` | `string` | 是 |  | Updated At |

### `TrainingPlanUpdateRequest`

修改训练方案请求。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `plan_name` | `string | null` | 否 |  | 训练名称，允许同名。 |
| `trainee` | `TraineeProfileRequest | null` | 否 |  | 修改学员画像会影响角色和后续阶段。 |
| `profile_type` | `string | null` | 否 |  | 修改客户画像类型会影响角色和后续阶段。 |
| `selected_fields` | `object | null` | 否 |  | 修改客户画像字段会影响角色和后续阶段。 |
| `scenario_description` | `string | null` | 否 |  | 修改场景会影响角色和后续阶段。 |
| `extra_details` | `string | null` | 否 |  | 修改补充细节会影响角色和后续阶段。 |
| `model_mode` | `string | null` | 否 |  | 只修改模型档位不强制重新生成。 |
| `role_confirm_card` | `object | null` | 否 |  | 只人工微调确认卡，不影响阶段。 |
| `visible_profile` | `object | null` | 否 |  | 人工微调可见画像，不影响阶段。 |
| `hidden_profile` | `object | null` | 否 |  | 修改隐藏画像会影响阶段和评分。 |
| `role_profile` | `object | null` | 否 |  | 修改扮演画像会影响阶段和评分。 |
| `training_purpose` | `string | null` | 否 |  | 人工修改训练宗旨，会影响后续训练会话展示。 |
| `round_limit` | `integer | null` | 否 |  | 人工修改训练轮数，限制在 1-100。 |
| `stages` | `array[GoalStage] | null` | 否 |  | 修改训练阶段会影响评分设置。 |
| `scoring_rules` | `object | null` | 否 |  | 只修改评分规则不影响前置步骤。 |

### `TrainingScoreResponse`

训练评分报告响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `score_id` | `string` | 是 |  | Score Id |
| `session_id` | `string` | 是 |  | Session Id |
| `total_score` | `integer` | 是 |  | Total Score |
| `level` | `string` | 是 |  | Level |
| `is_passed` | `boolean` | 是 |  | Is Passed |
| `general_score` | `integer` | 是 |  | General Score |
| `stage_score` | `integer` | 是 |  | Stage Score |
| `penalty_score` | `integer` | 是 |  | Penalty Score |
| `report` | `object` | 是 |  | Report |

### `TrainingSessionDetailResponse`

训练会话复盘详情。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `session` | `TrainingSessionSummaryResponse` | 是 |  | 对象模型：`TrainingSessionSummaryResponse` |
| `turns` | `array[TrainingTurnRecordResponse]` | 是 |  | Turns |
| `visible_profile` | `object` | 否 |  | Visible Profile |
| `hidden_profile` | `object` | 否 |  | Hidden Profile |
| `role_profile` | `object` | 否 |  | Role Profile |
| `role_confirm_card` | `object` | 否 |  | Role Confirm Card |
| `goal_setting` | `object` | 否 |  | Goal Setting |
| `knowledge_facts` | `array[string]` | 否 |  | Knowledge Facts |
| `score` | `TrainingScoreResponse | null` | 否 |  |  |

### `TrainingSessionListResponse`

训练会话历史分页响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `items` | `array[TrainingSessionSummaryResponse]` | 是 |  | Items |
| `total` | `integer` | 是 |  | Total |
| `page` | `integer` | 是 |  | Page |
| `page_size` | `integer` | 是 |  | Page Size |

### `TrainingSessionResponse`

训练会话响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `session_id` | `string` | 是 |  | 训练会话 ID，后续对话和评分都依赖它。 |
| `profile_id` | `string` | 是 |  | AI 客户画像 ID。 |
| `setting_id` | `string` | 是 |  | 训练设置 ID。 |
| `trainee_id` | `string` | 是 |  | 学员 ID。 |
| `training_mode` | `string` | 是 |  | 一期固定 open。 |
| `response_mode` | `string` | 是 |  | 当前会话默认回复模式。 |
| `current_stage_no` | `integer` | 是 |  | 当前阶段，一期固定 1。 |
| `status` | `string` | 是 |  | active / scoring / completed。 |
| `round_limit` | `integer` | 是 |  | 本场训练最大轮数。 |
| `opening_message` | `string | null` | 否 |  | AI 客户开场白，创建会话时返回给前端首屏展示。 |

### `TrainingSessionStartRequest`

开始训练会话请求。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `profile_id` | `string` | 是 |  | 使用哪个 AI 客户画像。 |
| `setting_id` | `string` | 是 |  | 使用哪个训练目标设置。 |
| `trainee_id` | `string` | 是 |  | 学员 ID。 |
| `response_mode` | `string` | 否 | `stream` | stream=流式，blocking=一次性。 |
| `model_mode` | `string | null` | 否 |  | 开场白使用的模型档位。 |

### `TrainingSessionSummaryResponse`

训练会话历史摘要。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `session_id` | `string` | 是 |  | Session Id |
| `trainee_id` | `string` | 是 |  | Trainee Id |
| `training_mode` | `string` | 是 |  | Training Mode |
| `response_mode` | `string` | 是 |  | Response Mode |
| `status` | `string` | 是 |  | Status |
| `round_limit` | `integer` | 是 |  | Round Limit |
| `answered_count` | `integer` | 是 |  | Answered Count |
| `total_score` | `integer | null` | 否 |  | Total Score |
| `level` | `string | null` | 否 |  | Level |
| `started_at` | `string` | 是 |  | Started At |
| `ended_at` | `string | null` | 否 |  | Ended At |
| `updated_at` | `string` | 是 |  | Updated At |

### `TrainingTurnRecordResponse`

训练会话中的单条对话记录。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `turn_id` | `string` | 是 |  | Turn Id |
| `session_id` | `string` | 是 |  | Session Id |
| `role` | `string` | 是 |  | Role |
| `content` | `string` | 是 |  | Content |
| `round_no` | `integer` | 是 |  | Round No |
| `stage_no` | `integer` | 是 |  | Stage No |
| `response_mode` | `string | null` | 否 |  | Response Mode |
| `response_seconds` | `number | null` | 否 |  | Response Seconds |
| `retrieved_chunk_ids` | `array[string]` | 否 |  | Retrieved Chunk Ids |
| `stage_decision` | `object` | 否 |  | Stage Decision |
| `coach_analysis` | `object` | 否 |  | 历史复盘时回显本轮教练分析。 |
| `created_at` | `string` | 是 |  | Created At |

### `TrainingTurnRequest`

提交学员回复请求。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `message` | `string` | 是 |  | 学员本轮回复。 |
| `response_mode` | `string` | 否 | `stream` | 本轮使用流式还是一次性。 |
| `model_mode` | `string | null` | 否 |  | 本轮 AI 客户回复使用的模型档位。 |

### `TrainingTurnResponse`

一次性训练对话响应。

| 字段 | 类型 | 必填 | 默认值 | 中文说明 |
| --- | --- | --- | --- | --- |
| `customer_reply` | `string` | 是 |  | AI 客户本轮回复。 |
| `current_stage_no` | `integer` | 是 |  | 当前阶段。 |
| `stage_status` | `string` | 是 |  | 阶段状态，例如 active / round_limit_reached。 |
| `session_status` | `string` | 是 |  | 会话状态，例如 active / scoring。 |
| `retrieved_chunk_ids` | `array[string]` | 否 |  | 本轮检索命中的训练知识切片 ID。 |
| `coach_analysis` | `object` | 否 |  | 给学员看的本轮销售教练分析。 |
| `response_seconds` | `number | null` | 否 |  | 后端生成本轮回复耗时，单位秒。 |
