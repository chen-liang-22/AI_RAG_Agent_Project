# 雪花算法主键 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 后续新增数据库业务主键统一改为纯雪花数字字符串，同时保持历史带前缀 ID 正常可用。

**Architecture:** 增加一个独立的 `infrastructure/id_generator.py`，由配置文件和环境变量控制 `worker_id`、epoch 和时钟回拨阈值。所有数据库业务主键生成点逐步从 `uuid.uuid4().hex` 切换到 `new_id()`，查询和删除逻辑继续把 ID 当普通字符串处理，不判断前缀。

**Tech Stack:** Python 3.12, FastAPI, SQLAlchemy, PyYAML, pytest, MySQL, Qdrant, MinIO.

---

## File Structure

| 文件 | 操作 | 职责 |
| --- | --- | --- |
| `config/id_generator.yml` | 新增 | 配置雪花算法 epoch、worker_id、时钟回拨阈值 |
| `infrastructure/id_generator.py` | 新增 | 进程内雪花 ID 生成器，提供 `new_id()` |
| `tests/test_id_generator.py` | 新增 | 覆盖纯数字、并发、worker_id 校验、时钟回拨、序列溢出 |
| `rag/knowledge_store.py` | 修改 | 替换字典、聊天、考试相关主键生成点 |
| `training/repository.py` | 修改 | 替换销售训练方案、画像、目标、会话、轮次、评分主键生成点 |
| `training/services/sales_training_service.py` | 修改 | 替换销售训练资料上传时的 `document_id`、`batch_id` |
| `api/routers/knowledge.py` | 修改 | 替换知识库确认入库时的 `document_id`，保留 `tmp_` 预览 ID |
| `api/services/indexing_services.py` | 修改 | 替换扫描 data 知识入库的 `document_id` |
| `api/services/auth_services.py` | 修改 | 替换系统用户 `user_id` |
| `app_v2/**` | 修改 | 替换 V2 中重复复制出来的主键生成点 |

---

### Task 1: Add Snowflake ID Generator Tests

**Files:**
- Create: `D:\d\PycharmProjects\AI_RAG_Agent_Project\tests\test_id_generator.py`
- Later implementation target: `D:\d\PycharmProjects\AI_RAG_Agent_Project\infrastructure\id_generator.py`

- [ ] **Step 1: Write failing tests**

Create `tests/test_id_generator.py`:

```python
from __future__ import annotations

import threading

import pytest

from infrastructure.id_generator import SnowflakeIdGenerator, new_id


def test_new_id_returns_numeric_string_with_safe_length():
    """new_id 应返回纯数字字符串，并且长度不超过现有 String(64) 字段。"""

    generated_id = new_id()

    assert generated_id.isdigit()
    assert len(generated_id) <= 64


def test_generator_creates_unique_ids_in_sequence():
    """连续生成大量 ID 时不能重复。"""

    generator = SnowflakeIdGenerator(worker_id=1)
    ids = [generator.next_id() for _ in range(10000)]

    assert len(ids) == len(set(ids))
    assert all(item.isdigit() for item in ids)


def test_generator_rejects_invalid_worker_id():
    """worker_id 超出 0-1023 范围时必须启动失败。"""

    with pytest.raises(ValueError, match="节点编号"):
        SnowflakeIdGenerator(worker_id=-1)

    with pytest.raises(ValueError, match="节点编号"):
        SnowflakeIdGenerator(worker_id=1024)


def test_generator_is_thread_safe():
    """多线程并发生成 ID 时不能重复。"""

    generator = SnowflakeIdGenerator(worker_id=2)
    generated_ids: list[str] = []
    lock = threading.Lock()

    def generate_many() -> None:
        local_ids = [generator.next_id() for _ in range(1000)]
        with lock:
            generated_ids.extend(local_ids)

    threads = [threading.Thread(target=generate_many) for _ in range(8)]
    for thread in threads:
        thread.start()
    for thread in threads:
        thread.join()

    assert len(generated_ids) == 8000
    assert len(generated_ids) == len(set(generated_ids))


def test_generator_rejects_large_clock_backwards():
    """系统时间严重回拨时必须拒绝生成 ID。"""

    times = iter([2000, 1900])
    generator = SnowflakeIdGenerator(
        worker_id=3,
        max_clock_backward_ms=5,
        time_func=lambda: next(times),
        sleep_func=lambda milliseconds: None,
    )

    generator.next_id()
    with pytest.raises(RuntimeError, match="系统时间回拨"):
        generator.next_id()


def test_generator_waits_for_small_clock_backwards():
    """系统时间轻微回拨时等待后继续生成。"""

    times = iter([2000, 1998, 2001])
    slept: list[int] = []
    generator = SnowflakeIdGenerator(
        worker_id=4,
        max_clock_backward_ms=5,
        time_func=lambda: next(times),
        sleep_func=lambda milliseconds: slept.append(milliseconds),
    )

    first_id = generator.next_id()
    second_id = generator.next_id()

    assert first_id != second_id
    assert slept == [2]


def test_generator_waits_next_millisecond_when_sequence_overflows():
    """同一毫秒序列号耗尽时，应等待下一毫秒。"""

    current_times = [3000]
    slept: list[int] = []

    def time_func() -> int:
        return current_times[0]

    def sleep_func(milliseconds: int) -> None:
        slept.append(milliseconds)
        current_times[0] += 1

    generator = SnowflakeIdGenerator(worker_id=5, time_func=time_func, sleep_func=sleep_func)

    ids = [generator.next_id() for _ in range(4097)]

    assert len(ids) == len(set(ids))
    assert slept == [1]
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m pytest tests\test_id_generator.py -q
```

Expected:

```text
ModuleNotFoundError: No module named 'infrastructure.id_generator'
```

- [ ] **Step 3: Commit failing tests**

```powershell
git add tests/test_id_generator.py
git commit -m "test: add snowflake id generator tests"
```

---

### Task 2: Implement Snowflake ID Generator

**Files:**
- Create: `D:\d\PycharmProjects\AI_RAG_Agent_Project\config\id_generator.yml`
- Create: `D:\d\PycharmProjects\AI_RAG_Agent_Project\infrastructure\id_generator.py`
- Test: `D:\d\PycharmProjects\AI_RAG_Agent_Project\tests\test_id_generator.py`

- [ ] **Step 1: Add configuration file**

Create `config/id_generator.yml`:

```yaml
# 是否启用雪花算法主键生成器。
enabled: true

# 雪花算法起始时间。固定后不要随意修改。
epoch: "2026-01-01T00:00:00+08:00"

# 本机节点编号。本地默认 0；生产多实例必须不同。
worker_id: 0

# 允许的轻微时钟回拨毫秒数。
max_clock_backward_ms: 5
```

- [ ] **Step 2: Implement generator**

Create `infrastructure/id_generator.py`:

```python
"""雪花算法主键生成器。

本模块只负责生成数据库业务主键。返回值统一是纯数字字符串，
历史带前缀 ID 继续由现有数据保留，不在这里处理。
"""

from __future__ import annotations

import os
import threading
import time
from dataclasses import dataclass
from datetime import datetime
from typing import Any, Callable

import yaml

from utils.logger_handler import logger
from utils.path_tool import get_abs_path


DEFAULT_EPOCH = "2026-01-01T00:00:00+08:00"
DEFAULT_WORKER_ID = 0
DEFAULT_MAX_CLOCK_BACKWARD_MS = 5
WORKER_ID_BITS = 10
SEQUENCE_BITS = 12
MAX_WORKER_ID = (1 << WORKER_ID_BITS) - 1
MAX_SEQUENCE = (1 << SEQUENCE_BITS) - 1
WORKER_ID_SHIFT = SEQUENCE_BITS
TIMESTAMP_SHIFT = WORKER_ID_BITS + SEQUENCE_BITS


@dataclass(frozen=True)
class IdGeneratorConfig:
    """雪花算法配置。"""

    enabled: bool
    epoch: str
    epoch_ms: int
    worker_id: int
    max_clock_backward_ms: int


class SnowflakeIdGenerator:
    """线程安全的雪花 ID 生成器。"""

    def __init__(
            self,
            *,
            worker_id: int,
            epoch_ms: int = 0,
            max_clock_backward_ms: int = DEFAULT_MAX_CLOCK_BACKWARD_MS,
            time_func: Callable[[], int] | None = None,
            sleep_func: Callable[[int], None] | None = None,
    ):
        """初始化生成器并校验节点编号。"""

        if worker_id < 0 or worker_id > MAX_WORKER_ID:
            raise ValueError(f"节点编号必须在 0-{MAX_WORKER_ID} 之间，当前值={worker_id}")
        if max_clock_backward_ms < 0:
            raise ValueError(f"时钟回拨容忍毫秒不能小于 0，当前值={max_clock_backward_ms}")

        self.worker_id = worker_id
        self.epoch_ms = epoch_ms
        self.max_clock_backward_ms = max_clock_backward_ms
        self.time_func = time_func or _current_time_ms
        self.sleep_func = sleep_func or _sleep_ms
        self._lock = threading.Lock()
        self._last_timestamp = -1
        self._sequence = 0

    def next_id(self) -> str:
        """生成纯数字字符串 ID。"""

        with self._lock:
            timestamp = self._next_valid_timestamp()
            if timestamp == self._last_timestamp:
                self._sequence = (self._sequence + 1) & MAX_SEQUENCE
                if self._sequence == 0:
                    timestamp = self._wait_next_millisecond(timestamp)
            else:
                self._sequence = 0

            self._last_timestamp = timestamp
            snowflake_id = (
                ((timestamp - self.epoch_ms) << TIMESTAMP_SHIFT)
                | (self.worker_id << WORKER_ID_SHIFT)
                | self._sequence
            )
            return str(snowflake_id)

    def _next_valid_timestamp(self) -> int:
        """获取可用毫秒时间，处理系统时间回拨。"""

        timestamp = self.time_func()
        if timestamp >= self._last_timestamp:
            return timestamp

        backward_ms = self._last_timestamp - timestamp
        if backward_ms > self.max_clock_backward_ms:
            logger.error(
                "[主键生成] 系统时间回拨超过阈值 上次毫秒=%s 当前毫秒=%s 回拨毫秒=%s",
                self._last_timestamp,
                timestamp,
                backward_ms,
            )
            raise RuntimeError(f"系统时间回拨超过阈值，回拨毫秒={backward_ms}")

        self.sleep_func(backward_ms)
        retry_timestamp = self.time_func()
        if retry_timestamp < self._last_timestamp:
            raise RuntimeError(f"系统时间回拨后仍不可用，回拨毫秒={self._last_timestamp - retry_timestamp}")
        return retry_timestamp

    def _wait_next_millisecond(self, current_timestamp: int) -> int:
        """等待进入下一毫秒，避免同毫秒序列号溢出。"""

        next_timestamp = self.time_func()
        while next_timestamp <= current_timestamp:
            self.sleep_func(1)
            next_timestamp = self.time_func()
        return next_timestamp


def new_id() -> str:
    """生成数据库业务主键，返回纯雪花数字字符串。"""

    return get_id_generator().next_id()


def get_id_generator() -> SnowflakeIdGenerator:
    """返回进程内单例雪花 ID 生成器。"""

    global _generator
    if _generator is None:
        with _generator_lock:
            if _generator is None:
                config = load_id_generator_config()
                _generator = SnowflakeIdGenerator(
                    worker_id=config.worker_id,
                    epoch_ms=config.epoch_ms,
                    max_clock_backward_ms=config.max_clock_backward_ms,
                )
                logger.info(
                    "[主键生成] 雪花算法配置加载完成 节点编号=%s 起始时间=%s",
                    config.worker_id,
                    config.epoch,
                )
    return _generator


def reset_id_generator_for_test() -> None:
    """重置单例生成器，仅供测试隔离使用。"""

    global _generator
    with _generator_lock:
        _generator = None


def load_id_generator_config(config_path: str | None = None) -> IdGeneratorConfig:
    """读取雪花算法配置，并应用环境变量覆盖。"""

    path = config_path or get_abs_path("config/id_generator.yml")
    raw_config = _read_yaml(path)
    epoch = str(os.getenv("ID_GENERATOR_EPOCH") or raw_config.get("epoch") or DEFAULT_EPOCH)
    worker_id = _read_int("ID_GENERATOR_WORKER_ID", raw_config.get("worker_id"), DEFAULT_WORKER_ID)
    max_clock_backward_ms = _read_int(
        "ID_GENERATOR_MAX_CLOCK_BACKWARD_MS",
        raw_config.get("max_clock_backward_ms"),
        DEFAULT_MAX_CLOCK_BACKWARD_MS,
    )
    enabled = _read_bool(raw_config.get("enabled"), True)
    return IdGeneratorConfig(
        enabled=enabled,
        epoch=epoch,
        epoch_ms=_parse_epoch_ms(epoch),
        worker_id=worker_id,
        max_clock_backward_ms=max_clock_backward_ms,
    )


def _read_yaml(config_path: str) -> dict[str, Any]:
    """读取 YAML 配置文件，不存在时返回空配置。"""

    if not os.path.exists(config_path):
        return {}
    with open(config_path, "r", encoding="utf-8") as config_file:
        data = yaml.safe_load(config_file) or {}
    if not isinstance(data, dict):
        raise ValueError(f"ID 生成器配置必须是对象结构：{config_path}")
    return data


def _read_int(env_name: str, config_value: Any, default_value: int) -> int:
    """读取整数配置，环境变量优先。"""

    raw_value = os.getenv(env_name)
    if raw_value is None or raw_value == "":
        raw_value = config_value
    if raw_value is None or raw_value == "":
        return default_value
    try:
        return int(raw_value)
    except (TypeError, ValueError) as exc:
        raise ValueError(f"配置不是有效整数 名称={env_name} 值={raw_value}") from exc


def _read_bool(config_value: Any, default_value: bool) -> bool:
    """读取布尔配置。"""

    if config_value is None or config_value == "":
        return default_value
    if isinstance(config_value, bool):
        return config_value
    return str(config_value).strip().lower() in {"1", "true", "yes", "on"}


def _parse_epoch_ms(epoch: str) -> int:
    """把 ISO 起始时间转换成毫秒时间戳。"""

    return int(datetime.fromisoformat(epoch).timestamp() * 1000)


def _current_time_ms() -> int:
    """返回当前毫秒时间戳。"""

    return int(time.time() * 1000)


def _sleep_ms(milliseconds: int) -> None:
    """按毫秒等待。"""

    time.sleep(milliseconds / 1000)


_generator: SnowflakeIdGenerator | None = None
_generator_lock = threading.Lock()
```

- [ ] **Step 3: Run generator tests**

Run:

```powershell
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m pytest tests\test_id_generator.py -q
```

Expected:

```text
7 passed
```

- [ ] **Step 4: Compile generator**

Run:

```powershell
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m py_compile infrastructure\id_generator.py
```

Expected: no output and exit code 0.

- [ ] **Step 5: Commit generator**

```powershell
git add config/id_generator.yml infrastructure/id_generator.py tests/test_id_generator.py
git commit -m "feat: add snowflake id generator"
```

---

### Task 3: Replace Low-Risk Identity Generation

**Files:**
- Modify: `D:\d\PycharmProjects\AI_RAG_Agent_Project\rag\knowledge_store.py`
- Modify: `D:\d\PycharmProjects\AI_RAG_Agent_Project\api\services\auth_services.py`
- Test: `D:\d\PycharmProjects\AI_RAG_Agent_Project\tests\test_api_app.py`

- [ ] **Step 1: Add import in `rag/knowledge_store.py`**

Add near existing imports:

```python
from infrastructure.id_generator import new_id
```

- [ ] **Step 2: Replace dictionary item IDs**

Replace both occurrences:

```python
dictionary_item_id = f"dict_{uuid.uuid4().hex}"
```

with:

```python
dictionary_item_id = new_id()
```

- [ ] **Step 3: Replace conversation IDs**

Replace:

```python
clean_conversation_id = f"conv_{uuid.uuid4().hex}"
```

with:

```python
clean_conversation_id = new_id()
```

There are two branches in `ensure_conversation`; both must use `new_id()`.

- [ ] **Step 4: Replace message IDs**

Replace:

```python
message_id = f"msg_{uuid.uuid4().hex}"
```

with:

```python
message_id = new_id()
```

- [ ] **Step 5: Add import in `api/services/auth_services.py`**

Add near existing imports:

```python
from infrastructure.id_generator import new_id
```

- [ ] **Step 6: Replace system user ID**

Replace:

```python
user_id=f"user_{uuid.uuid4().hex}",
```

with:

```python
user_id=new_id(),
```

- [ ] **Step 7: Run focused checks**

Run:

```powershell
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m py_compile rag\knowledge_store.py api\services\auth_services.py
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m pytest tests\test_id_generator.py tests\test_api_app.py::test_openapi_exposes_core_routes -q
```

Expected:

```text
tests/test_id_generator.py passes
test_openapi_exposes_core_routes passes
```

- [ ] **Step 8: Commit low-risk replacements**

```powershell
git add rag/knowledge_store.py api/services/auth_services.py
git commit -m "feat: use snowflake ids for users dictionaries and chat"
```

---

### Task 4: Replace Knowledge and Exam Identity Generation

**Files:**
- Modify: `D:\d\PycharmProjects\AI_RAG_Agent_Project\api\routers\knowledge.py`
- Modify: `D:\d\PycharmProjects\AI_RAG_Agent_Project\api\services\indexing_services.py`
- Modify: `D:\d\PycharmProjects\AI_RAG_Agent_Project\rag\knowledge_store.py`
- Test: `D:\d\PycharmProjects\AI_RAG_Agent_Project\tests\test_api_app.py`

- [ ] **Step 1: Add import in knowledge router**

Add:

```python
from infrastructure.id_generator import new_id
```

Do not replace preview upload IDs. This line must remain UUID-based because it is a temporary ID:

```python
upload_id = f"tmp_{uuid.uuid4().hex}"
```

- [ ] **Step 2: Replace confirmed knowledge document ID**

Replace:

```python
document_id = f"doc_{uuid.uuid4().hex}"
```

with:

```python
document_id = new_id()
```

- [ ] **Step 3: Add import in indexing service**

Add:

```python
from infrastructure.id_generator import new_id
```

- [ ] **Step 4: Replace data scan document ID**

Replace:

```python
document_id = f"doc_{uuid.uuid4().hex}"
```

with:

```python
document_id = new_id()
```

- [ ] **Step 5: Replace exam IDs in `rag/knowledge_store.py`**

Replace:

```python
clean_session_id = (session_id or "").strip() or f"exam_{uuid.uuid4().hex}"
```

with:

```python
clean_session_id = (session_id or "").strip() or new_id()
```

Replace:

```python
exam_question_id = f"exam_q_{uuid.uuid4().hex}"
```

with:

```python
exam_question_id = new_id()
```

- [ ] **Step 6: Run focused checks**

Run:

```powershell
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m py_compile api\routers\knowledge.py api\services\indexing_services.py rag\knowledge_store.py
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m pytest tests\test_id_generator.py tests\test_api_app.py::test_openapi_exposes_core_routes tests\test_api_app.py::test_preview_knowledge_file_reads_text_from_registered_document -q
```

Expected:

```text
all selected tests pass
```

- [ ] **Step 7: Commit knowledge and exam replacements**

```powershell
git add api/routers/knowledge.py api/services/indexing_services.py rag/knowledge_store.py
git commit -m "feat: use snowflake ids for knowledge and exam records"
```

---

### Task 5: Replace Sales Training Identity Generation

**Files:**
- Modify: `D:\d\PycharmProjects\AI_RAG_Agent_Project\training\repository.py`
- Modify: `D:\d\PycharmProjects\AI_RAG_Agent_Project\training\services\sales_training_service.py`
- Test: `D:\d\PycharmProjects\AI_RAG_Agent_Project\tests\test_sales_training_ingest_flow.py`

- [ ] **Step 1: Add import in training repository**

Add:

```python
from infrastructure.id_generator import new_id
```

- [ ] **Step 2: Replace repository-generated IDs**

Replace these fallback expressions:

```python
batch_id = values.get("batch_id") or f"batch_{uuid.uuid4().hex}"
plan_id = values.get("plan_id") or f"plan_{uuid.uuid4().hex}"
profile_id = values.get("profile_id") or f"profile_{uuid.uuid4().hex}"
setting_id = values.get("setting_id") or f"setting_{uuid.uuid4().hex}"
session_id = values.get("session_id") or f"session_{uuid.uuid4().hex}"
turn_id = values.get("turn_id") or f"turn_{uuid.uuid4().hex}"
score_id = values.get("score_id") or f"score_{uuid.uuid4().hex}"
```

with:

```python
batch_id = values.get("batch_id") or new_id()
plan_id = values.get("plan_id") or new_id()
profile_id = values.get("profile_id") or new_id()
setting_id = values.get("setting_id") or new_id()
session_id = values.get("session_id") or new_id()
turn_id = values.get("turn_id") or new_id()
score_id = values.get("score_id") or new_id()
```

- [ ] **Step 3: Add import in sales training service**

Add:

```python
from infrastructure.id_generator import new_id
```

- [ ] **Step 4: Replace upload document and batch IDs**

Replace:

```python
document_id = f"doc_{uuid.uuid4().hex}"
batch_id = f"batch_{uuid.uuid4().hex}"
```

with:

```python
document_id = new_id()
batch_id = new_id()
```

Do not replace this fallback file name because it is not a database primary key:

```python
return clean or f"training_{uuid.uuid4().hex}.txt"
```

- [ ] **Step 5: Run focused checks**

Run:

```powershell
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m py_compile training\repository.py training\services\sales_training_service.py
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m pytest tests\test_id_generator.py tests\test_sales_training_ingest_flow.py::test_training_upload_waits_for_manual_publish tests\test_sales_training_ingest_flow.py::test_training_delete_uses_document_asset_service tests\test_sales_training_ingest_flow.py::test_training_delete_legacy_batch_without_document_id -q
```

Expected:

```text
all selected tests pass
```

- [ ] **Step 6: Commit sales training replacements**

```powershell
git add training/repository.py training/services/sales_training_service.py
git commit -m "feat: use snowflake ids for sales training records"
```

---

### Task 6: Replace V2 Duplicate Identity Generation

**Files:**
- Modify: `D:\d\PycharmProjects\AI_RAG_Agent_Project\app_v2\application\knowledge_service.py`
- Modify: `D:\d\PycharmProjects\AI_RAG_Agent_Project\app_v2\application\knowledge\indexing_service.py`
- Modify: `D:\d\PycharmProjects\AI_RAG_Agent_Project\app_v2\application\training\sales_training_core.py`
- Test: `D:\d\PycharmProjects\AI_RAG_Agent_Project\tests\test_v2_legacy_dependency_guards.py`
- Test: `D:\d\PycharmProjects\AI_RAG_Agent_Project\tests\test_v2_training_no_legacy_service.py`

- [ ] **Step 1: Add imports**

Add to each V2 file that creates database business IDs:

```python
from infrastructure.id_generator import new_id
```

- [ ] **Step 2: Replace V2 knowledge document IDs**

In both V2 knowledge services, replace:

```python
document_id = f"doc_{uuid.uuid4().hex}"
```

with:

```python
document_id = new_id()
```

Do not replace temporary preview IDs:

```python
upload_id = f"tmp_{uuid.uuid4().hex}"
```

- [ ] **Step 3: Replace V2 sales training upload IDs**

Replace:

```python
document_id = f"doc_{uuid.uuid4().hex}"
batch_id = f"batch_{uuid.uuid4().hex}"
```

with:

```python
document_id = new_id()
batch_id = new_id()
```

Do not replace temporary file names:

```python
return clean or f"training_{uuid.uuid4().hex}.txt"
```

- [ ] **Step 4: Run V2 focused checks**

Run:

```powershell
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m py_compile app_v2\application\knowledge_service.py app_v2\application\knowledge\indexing_service.py app_v2\application\training\sales_training_core.py
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m pytest tests\test_v2_legacy_dependency_guards.py tests\test_v2_training_no_legacy_service.py -q
```

Expected:

```text
all selected tests pass
```

- [ ] **Step 5: Commit V2 replacements**

```powershell
git add app_v2/application/knowledge_service.py app_v2/application/knowledge/indexing_service.py app_v2/application/training/sales_training_core.py
git commit -m "feat: use snowflake ids in v2 services"
```

---

### Task 7: Add Regression Assertions for Numeric IDs

**Files:**
- Modify: `D:\d\PycharmProjects\AI_RAG_Agent_Project\tests\test_sales_training_ingest_flow.py`
- Modify: `D:\d\PycharmProjects\AI_RAG_Agent_Project\tests\test_api_app.py`

- [ ] **Step 1: Add assertion to sales training upload test**

In `test_training_upload_waits_for_manual_publish`, add:

```python
assert upload_result.document_id.isdigit()
assert upload_result.batch_id.isdigit()
```

Place these assertions immediately after:

```python
assert upload_result.document_id
```

- [ ] **Step 2: Add helper to API tests**

Add near the top of `tests/test_api_app.py`:

```python
def assert_numeric_id(value: str) -> None:
    """断言新增业务主键是纯数字字符串。"""

    assert value.isdigit()
    assert len(value) <= 64
```

- [ ] **Step 3: Add assertions to any existing creation tests**

Where `tests/test_api_app.py` creates a new document, conversation, exam session, or message and receives an ID, add:

```python
assert_numeric_id(response_data["document_id"])
```

Use the correct response key for the tested API. Do not add assertions to tests that use hardcoded legacy IDs such as `doc_test` or `conv_active_1`.

- [ ] **Step 4: Run regression tests**

Run:

```powershell
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m pytest tests\test_id_generator.py tests\test_api_app.py tests\test_sales_training_ingest_flow.py -q
```

Expected:

```text
selected tests pass, except any pre-existing database-isolation failures already documented outside this change
```

If a failure is caused by historical shared MySQL data and not by ID generation, record the exact failing test and rerun the focused tests from Tasks 2-6.

- [ ] **Step 5: Commit regression assertions**

```powershell
git add tests/test_api_app.py tests/test_sales_training_ingest_flow.py
git commit -m "test: assert new business ids are numeric"
```

---

### Task 8: Final Scan and Compatibility Verification

**Files:**
- Modify only if scan finds missed database primary-key UUID generation.
- Check: `D:\d\PycharmProjects\AI_RAG_Agent_Project\docs\雪花算法主键方案\雪花算法主键整改设计方案.md`

- [ ] **Step 1: Scan remaining UUID usage**

Run:

```powershell
rg -n "uuid\\.uuid4\\(|uuid4\\(" api rag training app_v2 scripts
```

Expected remaining allowed patterns:

```text
tmp_{uuid.uuid4().hex}
training_{uuid.uuid4().hex}.txt
scripts/migrate_local_files_to_minio.py historical compatibility code
```

No remaining database business primary key should use `uuid.uuid4()`.

- [ ] **Step 2: Compile changed modules**

Run:

```powershell
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m py_compile infrastructure\id_generator.py rag\knowledge_store.py training\repository.py training\services\sales_training_service.py api\routers\knowledge.py api\services\indexing_services.py api\services\auth_services.py app_v2\application\knowledge_service.py app_v2\application\knowledge\indexing_service.py app_v2\application\training\sales_training_core.py
```

Expected: no output and exit code 0.

- [ ] **Step 3: Run focused full verification**

Run:

```powershell
D:\d\PycharmProjects\AI_RAG_Agent_Project\.venv312\Scripts\python.exe -m pytest tests\test_id_generator.py tests\test_api_app.py::test_openapi_exposes_core_routes tests\test_api_app.py::test_preview_knowledge_file_reads_text_from_registered_document tests\test_sales_training_ingest_flow.py::test_training_upload_waits_for_manual_publish tests\test_sales_training_ingest_flow.py::test_training_delete_uses_document_asset_service tests\test_sales_training_ingest_flow.py::test_training_delete_legacy_batch_without_document_id tests\test_sales_training_ingest_flow.py::test_training_delete_batch_removes_document_asset_chain tests\test_v2_legacy_dependency_guards.py tests\test_v2_training_no_legacy_service.py tests\test_v2_knowledge_service.py::test_knowledge_service_confirm_upload_creates_and_indexes_through_repository tests\test_v2_system_service.py::test_system_service_creates_role_and_user_with_snowflake_ids -q
```

Expected:

```text
all selected tests pass
```

当前实测结果：

```text
24 passed
```

说明：历史销售训练批次没有 `document_id` 时，删除逻辑已走按 `batch_id` 清理训练向量和批次表的兼容分支，保证旧数据不因新主键规范返回 500。

- [ ] **Step 4: Verify historical ID compatibility manually**

Use an existing historical ID from local data, such as a `doc_xxx` or `batch_xxx`, and verify the service still accepts it as a normal string:

```powershell
# 示例：替换成真实存在的历史文档编号。
curl.exe http://127.0.0.1:8000/knowledge/files/doc_existing
```

Expected:

```text
HTTP 200 if the historical document exists, or HTTP 404 if it does not exist.
There must be no validation error saying the ID must be numeric.
```

- [ ] **Step 5: Commit final cleanup**

```powershell
git add config/id_generator.yml infrastructure/id_generator.py tests/test_id_generator.py rag/knowledge_store.py training/repository.py training/services/sales_training_service.py api/routers/knowledge.py api/services/indexing_services.py api/services/auth_services.py app_v2/application/knowledge_service.py app_v2/application/knowledge/indexing_service.py app_v2/application/training/sales_training_core.py tests/test_api_app.py tests/test_sales_training_ingest_flow.py
git commit -m "chore: complete snowflake id migration"
```

---

## Self-Review Checklist

- [ ] Spec coverage: generator, config, worker_id, pure numeric IDs, historical compatibility, Qdrant and MinIO compatibility are all covered by tasks.
- [ ] Placeholder scan: no `TBD`, `TODO`, `implement later`, or vague “add tests” instructions remain.
- [ ] Type consistency: `new_id()` always returns `str`; ID fields remain `String(64)` / `VARCHAR(64)`.
- [ ] Scope check: no table schema migration is planned.
- [ ] Compatibility check: legacy `doc_xxx` / `batch_xxx` / `conv_xxx` IDs remain accepted as plain strings.
